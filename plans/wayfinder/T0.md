---
id: T0
title: 现状架构全景梳理
type: grilling
labels: [wayfinder:ticket]
status: closed
claimed_by: wayfinder-session
grilling_locked: 2026-08-08
closed_at: 2026-08-08
blocked_by: []
blocks: []
phase: "0"
---

# 现状架构全景梳理

## Question

quanter 系统当前的完整架构全景，应以**什么视图集 + 什么形式**文档化，才能成为后续所有重构（engine 拆分 / 适配层引入）与三维扩展工单的**可靠公共地基**？需逐一敲定：

- (a) **视图集**——模块边界与依赖图、核心数据流（采集→lake→信号→计划→订单→成交→对账→持仓）、订单/计划/持仓状态机、技术债与已知缺口分布、进程/调度拓扑：哪些必须有、哪些可选？
- (b) **形式**——C4 模型（Context/Container/Component）、Mermaid 图、ASCII 数据流图、还是混合？
- (c) **落位**——`docs/` 下文件结构（如 `docs/architecture/`），与现有 `docs/`（data-source-of-truth / neckline-method / guardrails 等）如何归并？
- (d) **颗粒度**——全景图 vs 关键模块深剖的边界（engine/state_store/qmt/fetcher 是否各附深剖）。

## Context

- 来自 grilling 共识 #7（Phase 0 = 现状全景梳理）+ 动机 2（架构没文档化、心里没全景）。这是动机 2 的核心交付，也是 T1/T2/T3 的公共前提。
- 客观信号：核心 ~3.5 万行；engine.py 3437 行；模块众多（trading/data/discovery/backtest/broker/strategies/ops/infra/broadcast/config/compute_unit/experiment/presentation）。
- memory 已有大量分散决策（C1-C9 改造、SSoT、进程模型等），需**收敛为一张图**而非新造。
- 零破坏性（纯文档），与"演进优先"完全不冲突，立刻产出对外/对内对齐价值。

## Notes

- 主流程：`mattpocock-skills:grilling`（与用户定视图集/形式/落位）。✅ **grilling 已完成 2026-08-08，决策集见下 `## Decisions`**。
- 探查：codegraph/tokensave 索引**已就绪**（本工单建图时旧陈述「未就绪」已过时——`.codegraph/`+`.tokensave/` 均在，开局 hook 已返回 codegraph 上下文）。用 `codegraph_explore` / `tokensave_context`；全局 CLAUDE.md 强制**不得派 Explore 子代理**。
- 产出物（图/文档）链接到本工单 `## 产出物`，不粘贴正文。
- 完成后（产出阶段结束）回填 MAP 的 *Decisions so far* 与 Phase 0 工单链接，并关单（解除对 T1 的阻塞）。

## Decisions（grilling 锁定 · 2026-08-08）

> 与用户逐分支 grilling 达成共识（wayfinder-session，max effort）。本节是**决策集**（里程碑）；产出物见 `## 产出物`。**T0 不在此关单**——产出具备前继续阻塞 T1（T1 起步依赖 engine 当前态深剖）。

### (a) 视图集 — 8 个独立视图（全 MUST，不融合）

| # | 视图 | Mermaid 图种 | 单一归宿权威 |
|---|---|---|---|
| 1 | 系统上下文（Tushare/miniQMT/钉钉/schtasks/用户 边界） | `C4Context`（借 C4-L1 idiom） | 外部边界 |
| 2 | Module 边界 + 依赖图（13 包） | `flowchart` | **模块/依赖** |
| 3 | Core 数据流（采集→lake→信号→计划→订单→成交→对账→持仓） | `sequenceDiagram` + `flowchart` | **数据路径** |
| 4 | Process/调度拓扑（uvicorn 单例 + lifespan 补跑 + schtasks + discovery cron/daemon） | `flowchart` | 进程/调度结构 |
| 5 | 状态机（order / plan / position 三交易生命周期） | `stateDiagram-v2` | 状态迁移 |
| 6 | 技术债/缺口分布（engine 3437 / qmt 1540 / data 缺口 / SSoT 半成品） | `flowchart`（**只画债-bearing 模块子集 + 严重度色块**，非 #2 全图复制） | 债务标注 |
| 7 | SSoT 数据层（9 域 ER） | `classDiagram`（**= α 可视补集**，prose 权威在 `docs/data-source-of-truth.md`） | 数据模型图（prose 归 .md） |
| 8 | 控制/时间流（C6 时钟统一后各 job 时间语义） | `flowchart`（**只画带时间窗的 job 切片**，非 #4 全拓扑复制） | 时间窗标注 |

- **状态机范围**：order / plan / position 三个交易生命周期。broker-session 连接态归 #4 拓扑；discovery champion 不进（发现域非交易域）。

### (b) 形式 — topic-flat + Mermaid（单一渲染约定）

- **不采 C4 模型**：8 视图里 6 个是行为/流/状态类，C4 只处方「结构」，覆盖缺（#1 完美贴 L1、#2/#7 半贴 L2/L3、其余 5 个落框架外）。#1 仅**借 C4-L1 idiom**（非采模型）。
- **Mermaid 单一渲染**：原生 GitHub/VSCode 渲染、纯文本进 git、覆盖全图种（flowchart/sequenceDiagram/stateDiagram-v2/C4Context/classDiagram）。
- **单一归宿 + 跨图引用**（用户拍「乙」）：每事实只在一处权威定义（见上表「单一归宿权威」列），他处链接不重抄。
- 既有 ASCII 盒图（如 `docs/data-source-of-truth.md` L49-55）**不强制替换**；新增架构图全用 Mermaid。

### (c) 落位 — `docs/architecture/`

- `README.md` 索引 + **单一归宿映射表**（每事实 → 权威视图列明）。
- `01-system-context.md` … `08-control-time-flow.md`（编号前缀给阅读顺序）。
- `deep-dives/engine-current-state.md`（engine 当前态深剖，见 (d)）。
- `roadmap.md`（living stub，T1-T3 填充）。
- **#7 = α**：`classDiagram` 补 `docs/data-source-of-truth.md` 的可视缺；prose 权威不动、不双源。
- **版本策略**：活文档 + 文件头「最近复核 `<date>`」戳；**不开 v1/v2 子目录**（与单一归宿一致，git 即版本）。
- **活文档不动**：`guardrails.md` / `neckline-method.md` / `data-source-of-truth.md`（架构图引用）；一次性报告（`2026-08-03-...` / `research_digest.md`）不动；`docs/superpowers/` 历史归档不动（README 指向）。
- **过时文档 = 丙（删）**：`docs/data_pool.md` + `docs/caisen-methodology-summary.md`。**执行兜底**：删除前最后核对一次两文件正文确认无意外保留价值（用户与本文均未读其正文，due-diligence 一次），且全程 git 可恢复。

### (d) 颗粒度 — B（全景 + engine 深剖）

- T0 产出 = 8 全景 + `engine.py` **当前态**深剖（责任集群 + 调用图 + 拆分缝合点）。
- `qmt.py` / `state_store` / `fetcher` 深剖各下放 T2 / T6 / T13 作第一步（它们的重构排在 T1 后，现剖会立刻过时）。
- **engine 深剖 ticket 结构 = 触发式毕业 T0.1**：先在 T0 session 内做；中途若明显超载（3437 行逐职责拆解超过单 session 预算），毕业成 T0.1 子工单阻塞 T1。触发条件记录于此，毕业时建 T0.1 并 wire `T0.1 blocks T1`。

## 产出物（待产出阶段链接）

> grilling 决策已锁；下列文档的**产出**是下一阶段工作（8 视图 + engine 深剖 + 删档）。逐一产出后在此勾选链接；全部完成后关 T0（解除 T1 阻塞）并回填 MAP *Decisions so far*。

- [x] `docs/architecture/README.md`（索引 + 单一归宿映射表）✅ 2026-08-08
- [x] `docs/architecture/01-system-context.md`（#1）✅ 2026-08-08
- [x] `docs/architecture/02-module-dependencies.md`（#2）✅ 2026-08-08
- [x] `docs/architecture/03-data-flow.md`（#3）✅ 2026-08-08
- [x] `docs/architecture/04-process-topology.md`（#4）✅ 2026-08-08
- [x] `docs/architecture/05-state-machines.md`（#5）✅ 2026-08-08
- [x] `docs/architecture/06-tech-debt.md`（#6）✅ 2026-08-08
- [x] `docs/architecture/07-ssot-data-layer.md`（#7 = α，classDiagram）✅ 2026-08-08
- [x] `docs/architecture/08-control-time-flow.md`（#8）✅ 2026-08-08
- [~] engine 深剖 → **触发式毕业独立工单 [T0.1](T0.1.md)**（超载条件成立：8 视图 + roadmap 已产，3437 行深剖硬塞将失深）
- [x] `docs/architecture/roadmap.md`（living stub）✅ 2026-08-08
- [x] 删除 `docs/data_pool.md` ✅（删前核查：严重过时，仍写 AKShare/JQData 退役前架构 + 已退役脚本）
- [x] 删除 `docs/caisen-methodology-summary.md` ✅（用户乙判：坚持丙删；删前核查发现其为颈线法理论原典 `H=颈线−谷底`/`目标_n=颈线+n×H`，非退役组件，但用户决意删，git 可恢复）

## Resolution（grilling + 产出 · closed 2026-08-08）

**决策集** → 见上文 `## Decisions`（(a) 8 独立视图 / (b) topic-flat + Mermaid / (c) `docs/architecture/` + #7=α / (d) B + 触发式 T0.1）。

**产出**（`docs/architecture/`，全 Mermaid）：
- README（索引 + **单一归宿映射表**）+ #1 系统上下文 + #2 模块依赖（13 包 6 层 + trading↔broker/data/presentation 三处双向耦合）+ #3 数据流 + #4 进程拓扑 + #5 状态机（order/plan/position）+ #6 技术债（严重度色块）+ #7 SSoT（α classDiagram，补 `data-source-of-truth.md` 可视缺）+ #8 时间流 + roadmap stub。
- **engine.py 当前态深剖 → 触发式毕业 [T0.1](T0.1.md)**（超载条件成立：8 视图已产 + 3437 行深剖塞入将失深）；T0.1 接过对 [T1](T1.md) 的阻塞。
- 过时文档清理：`docs/data_pool.md` ✅ 删（删前核查确认严重过时——仍写 AKShare/JQData 退役前架构 + 已退役脚本）；`docs/caisen-methodology-summary.md` ✅ 删（用户乙判坚持丙删；注：删前核查发现其为颈线法理论原典非退役组件，但用户决意删，git 可恢复）。

**地基价值**：8 视图 + 单一归宿映射表是 T1/T2/T3 及横向工单（T6 / T9-T13）的公共参考。**T1 起步前须先完成 [T0.1](T0.1.md)**（engine 当前态深剖）。

**关联活文档**（不在本目录，被交叉引用）：`docs/data-source-of-truth.md`（#7 α prose 对象）/ `docs/neckline-method.md`（#3 信号阶段）/ `docs/guardrails.md`（#4 / #5 风控）。
