---
id: T13
title: data 完整性生产 gate + 自动补采重构
type: prototype
labels: [wayfinder:ticket]
status: done
blocked_by: [T12]
completed: 2026-08-11
blocks: []
phase: horizontal
graduated_from: [T5]
---

# data 完整性生产 gate + 自动补采重构

## Question

> ⚠️ **[T12](T12.md) 实证（2026-08-08）**：daily 双轨（`sync_daily_incremental` vs `sync_tushare.py daily`）+ 通用同步器**无写入前历史行数守卫**，致 a_shares_daily 被 `sync_tushare.py daily` 残片覆盖（**1020万行 → 3200行**）。本工单的 **④ daily 双轨收口** 与新增的**「写入前历史行数守卫」**须置顶为最高优先（防再发）。

按 [T5](T5.md) 根治方案重构 data 完整性闭环（5 项）：

1. **scan_integrity 升级为生产链强制 gate**（接 `ops/data_pipeline.py` T1/T2 + 每周全扫调度）；
2. **repair_gaps 升级为自动补采触发**（scan FAIL → 自动 repair，受配额/熔断守卫）；
3. **完整性维度收敛 `data/integrity.py`**（新增 D5 复权一致 / D6 列完整 / D7 跨湖时区 / D9 行数阈值告警）；
4. **daily 湖双轨收口**（`TUSHARE_DATASETS` 删 `"daily"` key，唯一写入口 = `sync_daily_incremental`）；
5. **repair_gaps 裸调 pro 接口改走 `_fetch_with_guard`**（限频守卫，防裸奔）。

用 prototype 验证 scan→repair 闭环 + gate 接生产链后**不阻断正常交易日**（gate FAIL 的降级语义：阻断交易 vs 跳过标的 vs 告警继续）。

## Context

毕业自 [T5](T5.md)。T5 根因：生产 gate 只校验实时性、scan/repair 是孤立 CLI 无调度、历史缺口被动跳过永不发现。**blocked by [T12](T12.md)**（先确认活债真因，否则在单日湖上重构 gate 无意义——find_gaps 返空会让 scan 看起来"全绿"）。

## Notes

- 大工单（5 项），领取时先 grilling 定**是否拆子工单**（scan 自动化 / repair 自动化 / 维度扩展 / 双轨收口 / 限频修复 各可独立）。
- CLAUDE.md 风控：repair 自动触发必须带配额 + 熔断（防补采雪崩触发 Tushare 限频，记忆 akshare-jqdata-retired 的限频教训）。
- 附带文档债（非本工单 scope，记入 MAP Not yet specified）：`docs/data_pool.md` 严重过时、data 湖缺 SSoT 文档。

## Resolution

- **T13-A（L1 写入侧安全网）完成于 2026-08-11**：写入前历史行数守卫 `assert_safe_overwrite`（SSoT）接入四个湖写入口（通用同步器 `_sync_single`/`_build_multiindex`、`sync_daily_incremental`、`repair_gaps`）+ daily 双轨收口（删 `TUSHARE_DATASETS["daily"]`，唯一写入口 = 增量脚本）+ freshness 行数骤降维度（sidecar 基线环比）。封死 T12 式残片覆盖静默抹除路径（三道保险：freshness 基线 / 守卫拒写 / freshness 骤降检测）。实现计划见 [docs/superpowers/plans/2026-08-10-t13-a-write-side-safety-net.md](../../docs/superpowers/plans/2026-08-10-t13-a-write-side-safety-net.md)。
- **T13-B（L2 scan 生产 gate + L3 自动补采闭环）合并于 2026-08-11**（commit `31304e9f`）：scan→repair 闭环——scan FAIL → 自动 repair，配额/熔断守卫齐全；W0/D1 顺手补 `sync_data_lake` 两处裸写接 safe_overwrite（`c4c88a87`）。
- **运行态尾巴显式转出（2026-08-15 技术债波次）**：
  1. **补采回路运行态已修**（CR-6 · 2026-08-15）：回路机械性复活（350/350 日补采零中断 + 熔断复位），详见该波次 Task 6 报告；
  2. **scan 停牌真值缺口 → 另立新工单**：16371 段 unjustified 中含长停牌真缺（铁证：`000029.SZ` 深深房A 湖内洞 2016-09-14~2020-11-06 共 1514 天，与其因恒大重组停牌四年、2020-11 复牌的公开事实完全吻合）——本地 `suspend_d.parquet` ground-truth 未覆盖历史长停牌区间，`scan_integrity` 误判 `suspend_justified=False`，50 段配额被永不可补的段占据，16371 段永难净收敛。需 Tushare `suspend_d` 全历史回补（或等效真值源）纳入 justified 判定。
- **诚实注记（未竟项如实登记）**：T13-C（D5 复权一致 / D6 列完整 / D7 跨湖时区 / D9 行数阈值告警，按原编号的独立维度扩展）**未整体落地**——部分语义已以别处形态存在（freshness 行数骤降在 T13-A 侧、前复权一致性在 `sync_daily_incremental` 内建），但四维度未作为 scan 契约独立成项。余项不随本工单虚销，转入 data 完整性后续工单（与上述停牌真值缺口同批评估）。

status: done（2026-08-11 A/B 主体合并；本 Resolution 运行态尾巴/未竟项补记于 2026-08-15 技术债波次 CR-9 对账）。
