---
id: T14
title: 恢复 a_shares_daily 全量 + 修 SOCKS 环境（P0 运维）
type: task
labels: [wayfinder:ticket]
status: closed
claimed_by: wayfinder-session
closed_at: 2026-08-08
blocked_by: []
blocks: []
phase: horizontal
graduated_from: [T12]
priority: P0
---

# 恢复 a_shares_daily 全量 + 修 SOCKS 环境（P0 运维）

## Question

执行 P0 运维 SOP，恢复 a_shares_daily 并修复环境根因（毕业自 [T12](T12.md)）：

1. **修环境**：`.venv310` 装 PySocks（`pip install pysocks` 或 `pip install requests[socks]`），或改用 HTTPS 代理；验证 `pro.trade_cal(...)` 不再抛 `InvalidSchema`。
2. **备份现况**：先备份当前工作区单日文件（`cp data_lake/a_shares_daily.parquet /tmp/daily_single_day_evidence.parquet`）——留作 T12 证据/调试。
3. **恢复全量**：`git checkout HEAD -- data_lake/a_shares_daily.parquet`（恢复 LFS 对象 d96839e，1020万行/2448天/2016-07-06~2026-08-04）；验证 shape ≈ (1020万, 6)、日期范围正确。
4. **增量补到今天**：`python data/tools/sync_daily_incremental.py`，验证最新日推进到今天、行数稳定（除权标的重算不破坏历史）。
5. **验收**：daily freshness 绿 + 跑一次 `scan_integrity.py` 确认无 300214 那种历史缺口。

## Context

[T12](T12.md) 实证：a_shares_daily 被 `sync_tushare.py daily` 残片覆盖（1020万→3200行），HEAD LFS 对象完整可恢复。这是 **P0 活债**——不解决则任何 data 完整性讨论建在沙地上，[T13](T13.md) 重构也无意义。纯运维（git + pip + 现有脚本），**不改代码**。

## Notes

- 恢复后**不要**再让 data_service sweep 触发 `sync_tushare.py daily`（治本归 [T13](T13.md)：daily 双轨收口 + 写入守卫）。
- CLAUDE.md 风控：恢复操作动数据文件，**执行前与用户确认**；增量跑完验证行数 ≥ 恢复前（1020万级）。
- 完成后回填 MAP，并把 [T5](T5.md) 的“待实证活债”项标已解决。

## Resolution（task · closed 2026-08-08）

**核心 P0 data 急救达成 ✅**：
- **全量恢复**：`git checkout HEAD -- data_lake/a_shares_daily.parquet` → **1020万行 / 454MB / 2016-07-06~2026-08-04 / 2448 天**（LFS 对象 d96839e 完整）。
- **增量补到最新交易日**：`sync_daily_incremental.py`（unset SOCKS 代理直连）→ **10,218,475 行 / 最新 2026-08-07 / 2451 天**（+16600 行，除权标的 0 只，无破坏）。
- 单日残片已备份：`data_lake/.syncing/T12_evidence_daily_single_day_20260808.parquet`（T12 证据）。
- 数据未被 repair_gaps 停顿破坏（验证 shape 完好）。

**环境根因定位（两层）**：
1. ✅ `.venv310` 缺 PySocks → 已装 `pysocks-1.7.1`（requests 不再 `InvalidSchema`）。
2. ⚠️ **SOCKS5 代理 `socks5://127.0.0.1:5001` 失联**（`[WinError 10061] 目标积极拒绝`，代理服务 8/7 起未跑）→ 单次 sync 用 `ALL_PROXY= ` 绕过直连成功，但**生产 schtasks 环境仍有 ALL_PROXY，下次调度会再失败**。

**遗留（毕业/归入）**：
- **[T15](T15.md)（毕业，grilling，P0）**：tushare 代理 env 根治（决策 A 启代理 / B 移除 ALL_PROXY / C 代码绕过）——**不解决则 schtasks 每日 sync 仍失败**，且若再触发 `sync_tushare.py daily` 全量重建路径会再次覆盖（[T13](T13.md) 写入守卫未做前）。
- **688277.SH / 920685.BJ 近期漏采未补**：`repair_gaps --auto` 跑 10min 无输出（性能/可用性问题，印证 [T5](T5.md) "孤立 CLI 难用"）→ 归 [T13](T13.md)（repair 自动化重构）。
- **scan_integrity 全量校验未完成**：同上，归 [T13](T13.md)（scan 接生产 gate）。

**结论**：P0 活债（历史抹除）已急救恢复，a_shares_daily 重新可信。但**环境代理问题（[T15](T15.md)）是下一个 P0**——不修则下一次 schtasks sync 会再次失败。
