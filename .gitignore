# IDE
.idea/
.vscode/

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
env/
.env

# 数据和日志
*.log
# 注：data/ 是源码目录（cleaner/clients/lake_reader/symbol_names 等），非数据——
# 数据在 data_lake/（见下）。历史曾误用 `data/` 忽略整个源码目录，已移除，源码正常入库。
# 回放历史记录落盘（caisen/replay_runs.py 写，运行时数据不入库）
replay_runs/
# tushare 因子缓存（data/cache/，运行时 parquet 缓存，不入库）
data/cache/

# 蔡森 CLI 运行时输出（plans/<date>.json 为 screen 子命令产物，不入库）
# 锚定根目录（/plans/），避免误伤 docs/superpowers/plans/ 设计文档
/plans/

# 依赖包
node_modules/
web/node_modules/

# 生产环境构建产物
dist/
web/dist/
dist-ssr/
*.local

# 日志文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 编辑器目录和文件
.idea
.vscode
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?

# ========================
# Python & 环境
# ========================
__pycache__/
*.py[cod]
*$py.class
*.so
venv/
.venv*/
.env
# EMT SDK 二进制库 + 压缩包（外部下载，不入库；doc/test 文本可入库）
emt_api_python/lib/
EMT_API_Python_*.zip

# ========================
# IDE & 本地配置
# ========================
.idea/
.vscode/
.claude/
*.swp
*.DS_Store

# ========================
# 测试 & 运行产物
# ========================
.coverage
.pytest_cache/
reports/*.html
reports/*.csv
reports/*.json
# 数据湖生成产物（parquet/shards，本地产物不入库）
data_lake/
qmt_data
# 本地运行日志（server/main.py LOG_CONFIG 落盘，回测链路日志，本地产物不入库）
logs/

.codegraph
.superpowers
.ref
# tokensave MCP 运行时索引（机器特定、持续变化的二进制 db，不入库）
.tokensave/

xtquant
emt_api_python
xtquant_250807.rar
EMT_API_Python_V2.27.0.zip
scripts/pages/

# 书籍扫描版(版权+大文件)
多空轉折一手抓.pdf
*.pdf
# 白皮书扫描原图（28MB PNG，同上版权+大文件理由，不入库；OCR 文本「多空转折策略白皮书.md」入库）
.book_pages/
# E2E 测试截图产物（tests/e2e/caisen_token_path.py 生成，按需本地查看，不入库）
tests/e2e/_*.png
