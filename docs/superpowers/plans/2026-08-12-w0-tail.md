# W0 收尾实施计划（H3 Phase C 尾巴 + D1 T13-B 尾巴）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 收掉技术债综合治理总纲（`specs/2026-08-11-tech-debt-governance-master-design.md`）W0 波次的两条尾巴——H3 Phase C 残余（C2d report 归口 + JSON 读侧窗口关闭）与 D1 T13-B 残余（3 处裸写守卫 + P1 宏观写语义 + P2/P3 守卫测试 + 过滤告警通道），为 W1-A（T2 keystone）清出干净安全网。

**Architecture:** 两组并行可序。组 A（H3）：C2d 把 plan 归因聚合从 experiment 叶子层下沉到 trading 层（底层 DB API `list_signals_with_meta_by_plan_date_range` 已备），experiment CLI report 退化为 deprecation 提示（守 layer 铁律 5）；关闭 `load_plan` 的 JSON 读侧 fallback（对齐 DG-5，生产只信 trade_event 表）。组 B（D1）：`sync_macro_credit` 改合并语义再接 write-side 守卫（P1 彻底治），`sync_data_lake` 两处裸写接守卫，P2/P3 补回归/守卫测试，过滤告警接在 engine 调用方（data 层零侵入）。

**Tech Stack:** Python 3.10（`.venv310`）、pytest、pandas/pyarrow、sqlite3（stdlib）。无新依赖。

## Global Constraints

- 全中文注释，像素级说明 Why（CLAUDE.md）。
- Karpathy 极简：纯标准库 + Protocol/ABC/dataclass，**不引入** DI 容器/事件总线/ORM/重型黑盒（spec §0.2 硬约束 2）。
- 量化风控红线：宏观 ffill-only（**绝无 bfill**，前视偏差防线，sync_macro 合并后仍守）；多账户 account_id 口径一致（spec §0.2 硬约束 3）。
- 行为等价红线（spec §0.2 硬约束 5）：load_plan 关 fallback 后消费方契约不变（无 SIGNAL → 返 None，pre_open 安全跳过）；sync_macro 合并在「完整区间」输入下与旧覆盖语义逐字一致。
- 渐进式（spec §0.2 硬约束 4）：每个 Task 独立 commit，小步可 revert。
- **file:line 基准 = 2026-08-12**（本计划勘察时）。spec §11 纪律：每个 Task 开工前 re-verify 行号是否漂移，以符号名定位为准。
- 测试运行：`.venv310/Scripts/python.exe -m pytest`（Windows）；合并门 = L1 全量绿 + 受影响 e2e 绿。

## File Structure

| 文件 | 职责 | 本计划改动 |
|---|---|---|
| `trading/plan_report.py`（新建） | plan 归因聚合（按 experiment_id 切片）+ CLI 入口 | **新建** `report_plan_attribution` + `__main__` |
| `experiment/cli.py` | 实验配置 CLI（纯叶子层） | `_load_all_plans`/`_report` 移除；report 子命令退化为 deprecation 提示 |
| `trading/trading_plan.py` | T-1 计划读取（DB 真相源） | `load_plan` 移除 JSON 读侧 fallback（L119-129） |
| `data/tools/sync_macro_credit.py` | 宏观信贷湖同步 | `sync_macro` 改合并语义 + 接 `safe_overwrite`（P1） |
| `data/tools/sync_data_lake.py` | daily 前复权湖（__main__ 已 deprecated） | `build_multiindex`/`main` 两处裸写接 `safe_overwrite` |
| `trading/engine.py` | 交易引擎（含 `_eod` 调 filter） | `_eod` 调 `filter_universe_by_continuity` 后加过滤告警（节流） |
| `tests/trading/test_plan_report.py`（新建） | plan 归因聚合测试 | **新建** |
| `tests/trading/test_trading_plan.py` | load_plan 测试 | fallback 用例改造（DB 异常→None） |
| `tests/trading/test_trading_plan_attribution.py` | 归因测试 | 改种 DB SIGNAL（不再走 JSON fallback） |
| `tests/test_sync_macro_credit.py` 或邻近 | 宏观同步测试 | 新增合并语义测试 |
| `tests/test_sync_cli.py` | select_keys 测试 | 新增 P2 回归 + P3 守卫 |

---

## 组 A：H3 Phase C 收尾

### Task 1：C2d plan 归因聚合下沉 trading 层（组 A · H3）

**Files:**
- Create: `trading/plan_report.py`
- Modify: `experiment/cli.py:31-66`（删 `_load_all_plans`）、`:69-98`（删 `_report`）、`:130-132`（report 子命令保留 argparse）、`:165-167`（main report 分支退化）
- Test: `tests/trading/test_plan_report.py`（新建）
- Modify: `tests/experiment/test_cli.py`（若存在 report 断言，改 deprecation）

**Interfaces:**
- Consumes: `trading.state_store.list_signals_with_meta_by_plan_date_range(since=None, until=None, *, db_path=None) -> list[dict]`（每项 `{symbol, plan_date, **meta}`；meta 即原 order_dict，含 `experiment_id`/`experiment_weight`/`order`）。**注意**：该 API 返回的每一项就是「一个 order/SIGNAL 行」，无需再 `for o in p["orders"]` 嵌套（与 experiment 旧 `_report` 的关键差异）。
- Produces: `trading.plan_report.report_plan_attribution(since: str | None = None) -> list[dict]`，每项 `{experiment_id: str, n: int, weight: float | None, symbols: list[str]}`，按 `experiment_id` 升序；缺 `experiment_id` 归「未归因」桶。

**背景（why 下沉）**：experiment 是纯配置叶子层（`test_experiment_pure_leaf` layer 守卫禁任何 trading import）。plan 真相源 = DB `trade_event(SIGNAL).meta`（DG-5）。聚合属 trading concern，下沉后 experiment CLI report 仅留 deprecation 提示，零 trading import。

- [ ] **Step 1: 写失败测试**（新建 `tests/trading/test_plan_report.py`）

```python
# -*- coding: utf-8 -*-
"""trading.plan_report 归因聚合测试（C2d 下沉）。"""
from trading import plan_report


def test_report_aggregates_by_experiment_id(monkeypatch):
    """按 experiment_id 聚合 SIGNAL meta：订单数/末次权重/去重标的。"""
    fake_metas = [
        {"symbol": "600000.SH", "plan_date": "2026-08-11",
         "experiment_id": "exp_a", "experiment_weight": 0.6, "order": {"symbol": "600000.SH"}},
        {"symbol": "000001.SZ", "plan_date": "2026-08-11",
         "experiment_id": "exp_a", "experiment_weight": 0.6, "order": {"symbol": "000001.SZ"}},
        {"symbol": "300001.SZ", "plan_date": "2026-08-11",
         "experiment_id": "exp_b", "experiment_weight": 0.4, "order": {"symbol": "300001.SZ"}},
    ]
    monkeypatch.setattr(
        "trading.plan_report.state_store.list_signals_with_meta_by_plan_date_range",
        lambda since=None, until=None, **kw: fake_metas,
    )
    rows = plan_report.report_plan_attribution()
    by_id = {r["experiment_id"]: r for r in rows}
    assert by_id["exp_a"]["n"] == 2
    assert by_id["exp_a"]["weight"] == 0.6
    assert sorted(by_id["exp_a"]["symbols"]) == ["000001.SZ", "600000.SH"]
    assert by_id["exp_b"]["n"] == 1


def test_report_unattributed_bucket(monkeypatch):
    """缺 experiment_id 的老单归「未归因」桶，不崩。"""
    monkeypatch.setattr(
        "trading.plan_report.state_store.list_signals_with_meta_by_plan_date_range",
        lambda since=None, until=None, **kw: [
            {"symbol": "600000.SH", "plan_date": "2026-08-11",
             "order": {"symbol": "600000.SH"}}],  # 无 experiment_id
    )
    rows = plan_report.report_plan_attribution()
    assert rows[0]["experiment_id"] == "未归因"
    assert rows[0]["n"] == 1


def test_report_since_filters_by_plan_date(monkeypatch):
    """since 透传给底层 API（按 plan_date 区间，非 mtime）。"""
    captured = {}
    monkeypatch.setattr(
        "trading.plan_report.state_store.list_signals_with_meta_by_plan_date_range",
        lambda since=None, until=None, **kw: captured.setdefault("since", since) or [],
    )
    plan_report.report_plan_attribution(since="2026-07-01")
    assert captured["since"] == "2026-07-01"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv310/Scripts/python.exe -m pytest tests/trading/test_plan_report.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'trading.plan_report'`。

- [ ] **Step 3: 实现 `trading/plan_report.py`**

```python
# -*- coding: utf-8 -*-
"""T-1 计划归因聚合（C2d 下沉 · 2026-08-12）。

物理意图：A/B 实验跑完后，研究员要回答「prod vs candidate 各自真下了多少单、占多少权重、
覆盖多少标的」。聚合维度是 experiment_id，数据源是 DB trade_event(SIGNAL).meta（DG-5 单一
真相源 = trade_event 表，非 plan_*.json）。

Why 从 experiment/cli.py 下沉到此：experiment 是纯配置叶子层（layer 铁律 5，
test_experiment_pure_leaf 禁任何 trading import），而 plan 数据在 trading.state_store，
归因天然是 trading concern。experiment CLI 的 report 子命令已退化为 deprecation 提示。

致命日期轴（红线）：底层 list_signals_with_meta_by_plan_date_range 按 plan_date
（trade_id 后缀 substr(-10)）区间查，**非 trade_event.timestamp**（= T 日盘后写入 ≠
计划日 T+1）——旧 _load_all_plans 扫 plan_*.json 按 mtime 恒错，本模块走 DB plan_date 才对。
"""
from __future__ import annotations

import argparse
import sys

from trading import state_store

# 未带 experiment_id 的老 order 归到这个桶，避免历史回测单子与新实验单子混淆归因。
_UNATTRIBUTED = "未归因"


def report_plan_attribution(since: str | None = None) -> list[dict]:
    """按 experiment_id 聚合 SIGNAL 计划：订单数 / 末次权重 / 去重标的。

    Args:
        since: 起始 plan_date（YYYY-MM-DD，含）；None = 全量。

    Returns:
        每项 {experiment_id, n, weight, symbols}，按 experiment_id 升序。weight 取该实验
        末次覆盖（单实验内 weight 恒定，跨 plan 随 promote/set-weight 更新）。
    """
    metas = state_store.list_signals_with_meta_by_plan_date_range(since=since)
    groups: dict[str, dict] = {}
    for m in metas:
        # 底层每项即一个 SIGNAL/order 行（含 experiment_id/experiment_weight/order）。
        eid = m.get("experiment_id") or _UNATTRIBUTED
        g = groups.setdefault(eid, {"n": 0, "weight": None, "symbols": set()})
        g["n"] += 1
        g["weight"] = m.get("experiment_weight")  # 末次覆盖
        sym = (m.get("order") or {}).get("symbol") or m.get("symbol")
        if sym:
            g["symbols"].add(sym)
    return [
        {"experiment_id": eid, "n": g["n"], "weight": g["weight"],
         "symbols": sorted(g["symbols"])}
        for eid, g in sorted(groups.items())
    ]


def _main(argv: list | None = None) -> int:
    """CLI 入口：python -m trading.plan_report [--since YYYY-MM-DD]。"""
    ap = argparse.ArgumentParser(prog="trading.plan_report", description="T-1 计划归因聚合（按 experiment_id）")
    ap.add_argument("--since", default=None, help="起始计划日 YYYY-MM-DD（含），留空全量")
    args = ap.parse_args(argv)
    rows = report_plan_attribution(since=args.since)
    # 表头定宽对齐，方便终端肉眼扫读（与旧 experiment report 同口径）
    print(f"{'experiment_id':30}{'订单数':>8}{'权重':>8}{'标的数':>8}")
    for r in rows:
        w = f"{r['weight']:.2f}" if r["weight"] is not None else "-"
        print(f"{r['experiment_id']:30}{r['n']:>8}{w:>8}{len(r['symbols']):>8}")
    return 0


if __name__ == "__main__":
    sys.exit(_main())
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv310/Scripts/python.exe -m pytest tests/trading/test_plan_report.py -v`
Expected: 3 PASS。

- [ ] **Step 5: experiment/cli.py report 退化（守 layer 铁律，不 import trading）**

删 `experiment/cli.py:31-66`（`_load_all_plans` 整函数）与 `:69-98`（`_report` 整函数）。把 `main` 的 report 分支（约 L165-167）改为：

```python
        elif args.cmd == "report":
            # C2d（2026-08-12）：plan 归因已下沉 trading 层（plan 真相源=DB trade_event，
            # experiment 是叶子层禁 trading import）。本子命令保留以维持 argparse 兼容，
            # 仅打印迁移提示，不聚合。
            print("plan 归因已迁移至 trading 层，请改用：python -m trading.plan_report --since YYYY-MM-DD")
            return 0
```

同时删除文件顶部因删函数而失效的 import（`glob` 若仅 `_load_all_plans` 用则删；`json` 仍被 create 命令用，保留）。`_UNATTRIBUTED` 常量若仅 `_report` 用则删。`_build_parser` 的 report 子解析器（L130-132）保留（维持 CLI 兼容）。

- [ ] **Step 6: 跑 experiment 测试确认 layer 守卫不破**

Run: `.venv310/Scripts/python.exe -m pytest tests/experiment/ -v`
Expected: 全 PASS。若 `test_experiment_pure_leaf` 之前就靠 report 不 import trading 通过——本步删 `_load_all_plans`/`_report` 后 experiment 更纯净，守卫更不可能破。若有测试断言 report 输出聚合表，改为断言 deprecation 提示。

- [ ] **Step 7: Commit**

```bash
git add trading/plan_report.py tests/trading/test_plan_report.py experiment/cli.py tests/experiment/
git commit -m "refactor(experiment): C2d plan 归因下沉 trading 层——experiment report 退化守 layer 铁律（W0/H3）"
```

---

### Task 2：关闭 load_plan JSON 读侧 fallback（组 A · H3 · DG-5）

> ⚠️ **风险提示**：`load_plan` 有 19 个调用者。大量测试用 `patch("...load_plan", return_value=...)` mock——**不受影响**。真正走 DB→JSON fallback 的测试是本 Task 改造对象。**Step 1 必须先核对 `save_plan_legacy` 是否写 SIGNAL 行**，它决定 ~50 处种子调用是否受波及。

**Files:**
- Modify: `trading/trading_plan.py:49-129`（`load_plan` 移除 L119-129 JSON 读取块）
- Modify: `tests/trading/test_trading_plan.py:134-173`（fallback/corrupt 用例改造）
- Modify: `tests/trading/test_trading_plan_attribution.py:32,47`（改种 DB SIGNAL）
- Read-only 核对: `tests/_legacy_plan_io.py`（`save_plan_legacy`）

**Interfaces:**
- `load_plan(date) -> dict | None` 契约微调：无 SIGNAL / DB 异常 → 返 `None`（不再读 JSON）。消费方（pre_open/place_take_profit/veto）已处理 None（保守跳过），契约不变。

- [ ] **Step 1: 核对 `save_plan_legacy` 是否写 SIGNAL 行**

Run: `grep -n "SIGNAL\|action.*=\|insert.*trade_event\|_seed" tests/_legacy_plan_io.py`

判定：
- **若 `save_plan_legacy` 写 SIGNAL 行**（meta 落 DB）→ 它的 ~50 处调用者种子的计划经 DB 路径可读，关 fallback 后仍绿。A2 仅改 3-4 个直接测 fallback 的用例。
- **若 `save_plan_legacy` 只写 JSON + CONFIRMED（不写 SIGNAL）** → 关 fallback 后这些种子经 DB 路径读不到 SIGNAL → 返 None → 相关测试红。此时需先给 `save_plan_legacy` 补「写 SIGNAL 行」（与 `_seed_db_signal` 同口径），再关 fallback。**据此分叉：影响小则本 Task 内闭合；影响大则本 Task 仅做 save_plan_legacy 补 SIGNAL + 关 fallback 的 DB 异常分支（保留无 SIGNAL 时不读 JSON 已是 None），JSON 损坏用例后续单独 Task。**

把 Step 1 的 grep 结论记到 commit message（「save_plan_legacy 写/不写 SIGNAL」），供 review。

- [ ] **Step 2: 写新测试（DB 异常 → None，不再 fallback JSON）**

在 `tests/trading/test_trading_plan.py` 把 `test_load_plan_db_exception_falls_back_to_json`（L134）改为：

```python
def test_load_plan_db_exception_returns_none(tmp_path, monkeypatch):
    """C3 收尾（DG-5）：DB 读异常时 load_plan 返 None，不再回退 JSON（窗口已关闭）。

    物理：生产只信 trade_event 表。DB 异常 = 真相源不可达，pre_open 据 None 安全跳过
    （不挂单），不再读可能过时的 plan_*.json 镜像。
    """
    import trading.trading_plan as tp
    from trading import state_store

    def _boom(*a, **kw):
        raise RuntimeError("DB 不可达")
    monkeypatch.setattr(state_store, "list_signals_with_meta_by_plan_date", _boom)
    monkeypatch.setattr(tp, "_plan_path", lambda d: tmp_path / f"plan_{d}.json")
    (tmp_path / "plan_2026-08-05.json").write_text('{"date":"2026-08-05","orders":[]}', encoding="utf-8")
    # JSON 存在但不再被读 → DB 异常 → None
    assert tp.load_plan("2026-08-05") is None
```

- [ ] **Step 3: 跑确认失败**

Run: `.venv310/Scripts/python.exe -m pytest tests/trading/test_trading_plan.py::test_load_plan_db_exception_returns_none -v`
Expected: FAIL（当前实现 fallback 返 JSON dict）。

- [ ] **Step 4: 移除 `load_plan` JSON 读侧 fallback**

`trading/trading_plan.py`：删除 L119-129（`# —— JSON 回退 ——` 整块，含 `_plan_path`/`p.exists()`/`json.loads`）。把 L112-117 的 except 块改为：DB 异常 → 记 CRITICAL + 返 None（不再 fallback）。改后 except 块：

```python
    except Exception:
        # DG-5 收尾（2026-08-12）：生产只信 trade_event 表，JSON 读侧窗口关闭。
        # DB 读异常 = 真相源不可达，记 CRITICAL 告警 + 返 None（pre_open 安全跳过不挂单），
        # 不再回退读 plan_*.json（C3 后无生产写盘方，JSON 已非真相源）。
        logger.exception("load_plan 读 DB SIGNAL 失败，返 None（JSON 窗口已关闭）")
        return None
```

`load_plan` 末尾（原 JSON 块之后）的「无 SIGNAL」路径：DB 正常但 `metas` 为空时，函数应返 `None`。当前结构里 `if metas:` 为假则落到 JSON 块——删 JSON 块后，需在 `try` 块内 `metas` 判空处显式 `return None`。检查 L86 `if metas:` 的 else 分支：补 `return None`（无 SIGNAL = 无计划）。

同步更新 `load_plan` docstring 的「回退窗口」段（L70-77）→ 改为「DG-5 收尾：JSON 读侧窗口已关闭，无 SIGNAL / DB 异常均返 None」。

`_plan_path`（L37-46）：若删除 JSON 块后 `_plan_path` 在本模块无其他调用者（grep 确认），保留它（`tests/_legacy_plan_io._plan_path` 同口径读取仍需），但 docstring 标注「仅供测试 legacy shim 路径计算，load_plan 不再读 JSON」。

- [ ] **Step 5: 改造走 fallback 路径的归因测试**

`tests/trading/test_trading_plan_attribution.py:32,47`——两处注释明示「无 DB SIGNAL → 回退 JSON」。改为种 DB SIGNAL（用该文件已有或 `test_trading_plan.py:_seed_db_signal` 同款 helper 写 SIGNAL+experiment_id meta），让 load_plan 走 DB 路径读到 experiment 归因字段。若该文件无种 DB 的 helper，从 `test_trading_plan.py` 抽/复制 `_seed_db_signal`。

`test_trading_plan.py:166 test_load_plan_corrupt`——JSON 损坏用例：JSON 窗口关闭后，损坏 JSON 不再被读。改为：种损坏 JSON + 无 DB SIGNAL → load_plan 返 None（不再测「损坏 JSON 返 None」的 fallback 语义，改为「无 SIGNAL 返 None」）。

`test_veto_plan_db.py:232` 附近——grep 确认其是否断言 JSON fallback；若是，改种 DB SIGNAL。

- [ ] **Step 6: 跑 trading 全量确认绿**

Run: `.venv310/Scripts/python.exe -m pytest tests/trading/ -q`
Expected: 全绿。若 Step 1 判定 save_plan_legacy 不写 SIGNAL 且 Step 5 未覆盖某种子测试导致红——回到 Step 1 分叉：给 `save_plan_legacy` 补 SIGNAL 写入（与 `_seed_db_signal` 同口径），再跑。

- [ ] **Step 7: Commit**

```bash
git add trading/trading_plan.py tests/trading/test_trading_plan.py tests/trading/test_trading_plan_attribution.py tests/trading/test_veto_plan_db.py
git commit -m "refactor(trading): load_plan 关闭 JSON 读侧 fallback——生产只信 trade_event 表（W0/H3 DG-5）"
```
commit body 注明 Step 1 的核对结论（save_plan_legacy 写/不写 SIGNAL）。

---

## 组 B：D1 T13-B 收尾

### Task 3：sync_macro_credit 改合并语义 + 接 safe_overwrite（组 B · D1 · P1）

**Files:**
- Modify: `data/tools/sync_macro_credit.py:199-213`（`sync_macro`）
- Test: `tests/test_sync_macro_credit.py`（若不存在则新建；先 grep 确认现有宏观测试文件名）

**Interfaces:**
- Consumes: `data.integrity.safe_overwrite(lake_path: str, new_df: pd.DataFrame, *, min_ratio: float = 0.9) -> None`（自动读 `QUANTER_FORCE_WRITE` env；骤降抛 `WriteGuardError`）。
- Produces: `sync_macro(start, end, out=None) -> None`，语义从「整体覆盖」改为「合并（new 覆盖同期 + 保留现有区间外历史）+ safe_overwrite」。

**Why**（P1）：旧 `to_parquet(out)` 整体覆盖，接入 write-side 守卫后，窄窗口增量重采（行数 << 现有）会触发骤降 `WriteGuardError` → 宏观湖永久断更。合并语义下行数单调≥现有，窄窗口也不误拒，彻底治 P1。前视红线：合并后**仅 ffill**（无 bfill）。

- [ ] **Step 1: 写失败测试**

先确认测试文件：`grep -rln "sync_macro\|fetch_macro_series" tests/`。若需新建 `tests/test_sync_macro_credit.py`：

```python
# -*- coding: utf-8 -*-
"""sync_macro 合并语义 + write-side 守卫测试（P1）。"""
import pandas as pd
from data.tools.sync_macro_credit import sync_macro


def test_sync_macro_merges_narrow_window_keeps_history(tmp_path, monkeypatch):
    """窄窗口重采不丢历史：existing 有 [d0..d100]，new 只覆盖 [d50..d60]，
    合并后仍保留 d0..d100（new 同期值更新），不因窄窗口被守卫拒写或抹除历史。"""
    out = tmp_path / "macro.parquet"
    # 现有：100 个工作日，shrzgm 列
    idx_old = pd.bdate_range("2026-01-01", periods=100)
    existing = pd.DataFrame({"shrzgm": range(100), "M1M2_gap": [0.1] * 100}, index=idx_old)
    existing.index.name = "date"
    existing.to_parquet(out)

    # new：窄窗口 [d50..d60]，行数 11 << 100（旧覆盖语义会触发骤降守卫）
    idx_new = pd.bdate_range("2026-01-01") + pd.Timedelta(days=50 * 7 // 5)  # 任取窄段
    idx_new = idx_old[50:61]
    new_df = pd.DataFrame({"shrzgm": range(1000, 1011), "M1M2_gap": [0.5] * 11}, index=idx_new)
    new_df.index.name = "date"
    monkeypatch.setattr("data.tools.sync_macro_credit.fetch_macro_series", lambda s, e: new_df)
    monkeypatch.setattr("data.tools.sync_macro_credit.LAKE_CONFIG", {"lakes": {"macro": str(out)}})

    sync_macro("2026-03-01", "2026-03-15")  # 窄窗口

    got = pd.read_parquet(out)
    assert len(got) >= 100, "窄窗口合并不应丢历史"
    assert "shrzgm" in got.columns
    # new 同期值已更新（d50..d60 的 shrzgm = 1000+）
    seg = got.loc[idx_old[50]:idx_old[60], "shrzgm"]
    assert (seg == pd.Series(range(1000, 1011), index=idx_old[50:61])).all()
```

- [ ] **Step 2: 跑确认失败**

Run: `.venv310/Scripts/python.exe -m pytest tests/test_sync_macro_credit.py -v`
Expected: FAIL（当前整体覆盖，窄窗口要么因守卫拒写——若已接——要么抹除历史只留 11 行）。

- [ ] **Step 3: 实现 sync_macro 合并 + safe_overwrite**

`data/tools/sync_macro_credit.py:199-213` 改为：

```python
def sync_macro(start: str, end: str, out: str | None = None) -> None:
    """落 data_lake/macro_credit.parquet（DatetimeIndex，列含 shrzgm + M1M2_gap）。

    P1 修（2026-08-12）：改合并语义——读现有湖 + new 按索引合并（new 同期覆盖、保留现有
    区间外历史），再 safe_overwrite 全量落盘。彻底治「窄窗口增量重采触发 WriteGuardError
    永久断更」：合并后行数单调≥现有，safe_overwrite 不误拒。

    前视红线（不变）：合并后仅 ffill（用过去解释现在），绝无 bfill（不回填未来月度值）。
    """
    from data.integrity import safe_overwrite
    out = out or LAKE_CONFIG["lakes"]["macro"]
    df = fetch_macro_series(start, end)
    if df.empty:
        print("宏观数据为空，跳过")
        return
    # 合并：读现有湖，new 同期覆盖、保留现有区间外历史。
    if os.path.exists(out):
        try:
            existing = pd.read_parquet(out)
            # concat 后按索引去重（保留 new = keep='last'），new 同期值覆盖 existing。
            combined = pd.concat([existing, df])
            combined = combined[~combined.index.duplicated(keep="last")].sort_index()
            # 合并衔接处可能产 NaN（区间拼接缝隙）→ 仅 ffill 兜底，仍无 bfill（前视红线）。
            for col in combined.columns:
                combined[col] = combined[col].ffill()
            df = combined
        except Exception:
            # 现有湖读失败（损坏/非预期 schema）→ 回退全量重采覆盖 + 告警，不阻断同步。
            print(f"⚠ 读现有宏观湖失败，回退全量覆盖：{out}")
    os.makedirs(os.path.dirname(out), exist_ok=True)
    safe_overwrite(out, df)  # write-side 守卫：合并后行数≥现有，不误拒
    print(f"宏观湖写入：{out}，{len(df)} 行，列={list(df.columns)}")
```

（文件顶部 `import os`、`import pandas as pd` 已有，确认；`safe_overwrite` 函数内 import 保持局部。）

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv310/Scripts/python.exe -m pytest tests/test_sync_macro_credit.py -v`
Expected: PASS。

- [ ] **Step 5: 跑既有宏观相关测试确认无回归**

Run: `.venv310/Scripts/python.exe -m pytest tests/test_macro*.py tests/test_credit_regime*.py -q 2>nul || .venv310/Scripts/python.exe -m pytest tests/ -k "macro or credit" -q`
Expected: 全绿（合并语义在完整区间输入下与旧覆盖逐字一致——行为等价）。

- [ ] **Step 6: Commit**

```bash
git add data/tools/sync_macro_credit.py tests/test_sync_macro_credit.py
git commit -m "fix(data): sync_macro 改合并语义+接 safe_overwrite——治 P1 窄窗口断更（W0/D1）"
```

---

### Task 4：sync_data_lake 两处裸写接 safe_overwrite（组 B · D1）

**Files:**
- Modify: `data/tools/sync_data_lake.py:154-159`（`build_multiindex`）、`:178-182`（`main` shard 写）
- Test: 既有 `tests/test_sync_data_lake.py`

**背景**：本脚本 `__main__` 已 deprecated（L186-194，2026-07-25 scripts 收敛），但 `build_multiindex`/`fetch_qfq` 函数保留供测试（`test_daily_migration_parity` 等）与潜在 import。两处裸 `to_parquet` 接守卫防误用（spec §3.3.3 deferred 项）。

- [ ] **Step 1: build_multiindex 接守卫**

`data/tools/sync_data_lake.py:157-158`，在 `big.to_parquet(out, engine="pyarrow")` 前加：

```python
    os.makedirs(os.path.dirname(out), exist_ok=True)
    # write-side 守卫（T13-A deferred 收尾）：防残片覆盖。build_multiindex 是全量重算合并，
    # 正常行数稳定；骤降（new < 现有×0.9）= 疑残片/部分回采 → 拒写保护历史。
    from data.integrity import safe_overwrite
    safe_overwrite(out, big)
    big.to_parquet(out, engine="pyarrow")
```

- [ ] **Step 2: main shard 写接守卫**

`:181 df.to_parquet(shard)` 是**单标的 shard**（首次写/续传覆盖单标的文件）。shard 是按标的的小文件，首次写无基线（safe_overwrite 放行），续传覆盖时若新 shard 残片（行数骤降）拒写。加守卫：

```python
        from data.integrity import safe_overwrite
        safe_overwrite(shard, df)
        df.to_parquet(shard)
```

（两处 `safe_overwrite` 调用紧贴对应 `to_parquet` 前；局部 import 保持与 sync_macro 同范式。）

- [ ] **Step 3: 跑既有测试确认绿（守卫首次写放行不破坏）**

Run: `.venv310/Scripts/python.exe -m pytest tests/test_sync_data_lake.py -v`
Expected: 全绿（tmp_path 首次写，safe_overwrite 无基线放行）。若某用例刻意写残片验证覆盖——加 `QUANTER_FORCE_WRITE=1` 或 monkeypatch `safe_overwrite` 为 no-op。

- [ ] **Step 4: Commit**

```bash
git add data/tools/sync_data_lake.py
git commit -m "fix(data): sync_data_lake 两处裸写接 safe_overwrite——T13-A deferred 收尾（W0/D1）"
```

---

### Task 5：select_keys 回归测试（组 B · D1 · P2）

**Files:**
- Test: `tests/test_sync_cli.py`（已存在，P2 修复 commit `58888bc6` 在此）

**背景**：`data/sync_cli.py:62-78 select_keys` 已修（过滤退役 key + `_unavailable`，防 `TUSHARE_DATASETS[k]` KeyError）。spec §3.3.4 P2 要求「复核修复完整 + 补回归测试」。

- [ ] **Step 1: 确认 P2 修复现状（re-verify）**

Run: `grep -n "select_keys\|_unavailable" tests/test_sync_cli.py`
确认是否已有 `--keys daily` 不崩的用例。若无，Step 2 补。

- [ ] **Step 2: 补回归测试**（追加到 `tests/test_sync_cli.py`）

```python
def test_select_keys_retired_key_no_keyerror():
    """P2 回归：--keys daily（退役 key）不 KeyError，select_keys 过滤掉退役/无效 key。"""
    from data.sync_cli import select_keys
    from config import TUSHARE_DATASETS
    # daily 已退役（T13-A 双轨收口）——传退役 key 不崩，且被过滤出结果
    sel = select_keys(all_keys=False, keys=["daily", "weekly"], quota=None)
    assert "daily" not in sel, "退役 key 应被过滤"
    assert "weekly" in sel


def test_select_keys_retired_with_quota_no_keyerror():
    """P2 回归：退役 key + quota 组合不 KeyError（原崩溃路径 --keys daily --quota basic）。"""
    from data.sync_cli import select_keys
    sel = select_keys(all_keys=False, keys=["daily"], quota="basic")
    assert sel == [], "退役 key 经 quota 过滤后为空，不崩"
```

- [ ] **Step 3: 跑确认通过**

Run: `.venv310/Scripts/python.exe -m pytest tests/test_sync_cli.py -k select_keys -v`
Expected: PASS。

- [ ] **Step 4: Commit**

```bash
git add tests/test_sync_cli.py
git commit -m "test(data): select_keys 退役 key 回归测试——P2 复核（W0/D1）"
```

---

### Task 6：_unavailable 守卫测试（组 B · D1 · P3）

**Files:**
- Test: `tests/test_sync_cli.py`

**背景**（spec §3.3.4 P3，按既定推荐）：退役 key 保留 `_unavailable` 标记（向后兼容 + 留存退役原因），补 select_keys 对 `_unavailable` 漏过滤的防再生守卫。

- [ ] **Step 1: 写守卫测试**（追加到 `tests/test_sync_cli.py`）

```python
def test_select_keys_filters_unavailable_marker(monkeypatch):
    """P3 守卫：_unavailable 标记的 key 一律被过滤，防 select_keys 漏过滤再生。"""
    from data import sync_cli
    fake = {"good": {"quota_type": "basic"}, "bad": {"quota_type": "basic", "_unavailable": True}}
    monkeypatch.setattr(sync_cli, "TUSHARE_DATASETS", fake)
    sel = sync_cli.select_keys(all_keys=True, keys=None, quota=None)
    assert sel == ["good"], "_unavailable key 必须被过滤"
```

- [ ] **Step 2: 跑确认通过**

Run: `.venv310/Scripts/python.exe -m pytest tests/test_sync_cli.py -k unavailable -v`
Expected: PASS。

- [ ] **Step 3: Commit**

```bash
git add tests/test_sync_cli.py
git commit -m "test(data): select_keys _unavailable 漏过滤守卫——P3 防再生（W0/D1）"
```

---

### Task 7：filter_universe_by_continuity 过滤告警通道（组 B · D1 · ②）

**Files:**
- Modify: `trading/engine.py`（`_eod` 调 `filter_universe_by_continuity` 处——先 grep 定位）
- Test: `tests/trading/test_engine*.py`（邻近 _eod 测试）

**背景**（spec §3.3.5 ②）：当前被过滤标的仅 `_log.warning`（`data/integrity.py:413`），无钉钉/CRITICAL 通道——残数据标的被过滤「无声」。**设计决策**：为不污染 data 层（layer 干净），告警接在 **engine 调用方**（filter 前后对比 `len(universe) vs len(clean)`，超阈值 → `_alert_critical` 节流），`filter_universe_by_continuity` 零改动。

- [ ] **Step 1: 定位 engine 中 filter 调用点**

Run: `grep -n "filter_universe_by_continuity" trading/engine.py`
确认调用处（codegraph 示 `_eod → filter_universe_by_continuity`）。读该处上下文（前后 ~20 行），看清 universe/clean 变量名与 `_alert_critical` 是否在同作用域可见。

- [ ] **Step 2: 写失败测试**（在邻近 _eod 测试文件加）

```python
def test_eod_alerts_when_continuity_filter_drops_many(monkeypatch):
    """② 告警通道：filter 过滤标的数超阈值 → _alert_critical 被调（残数据有声）。"""
    # 种 10 只 universe，filter 只放行 3 只（过滤 7 > 阈值）→ 告警
    ...
    # monkeypatch filter_universe_by_continuity 返 3 只；monkeypatch _alert_critical 捕获
    # 断言 _alert_called 被调，msg 含过滤数
```
（具体 setup 参考同文件既有 _eod 测试的 monkeypatch 范式；阈值用模块常量或可注入，便于测试。）

- [ ] **Step 3: 跑确认失败**

Run: `.venv310/Scripts/python.exe -m pytest tests/trading/test_engine*.py -k continuity_filter -v`
Expected: FAIL（当前无告警逻辑）。

- [ ] **Step 4: 实现 engine 调用方告警（节流）**

在 `filter_universe_by_continuity` 调用后加（具体变量名按 Step 1 实况）：

```python
            clean = filter_universe_by_continuity(universe, df_map, window, susp, trade_days)
            # ② 告警通道（2026-08-12）：被过滤标的数超阈值 → CRITICAL 钉钉（残数据有声）。
            # Why 在 engine 调用方而非 data/integrity：data 层保持纯净（不 import infra.notifier），
            # 告警统一走 _alert_critical（复用 infra.notifier 通道，与 _health_guard 同链）。
            dropped = len(universe) - len(clean)
            if dropped >= _CONTINUITY_FILTER_ALERT_FLOOR:  # 模块级常量，如 5
                _alert_critical(
                    f"完整性 gate 过滤 {dropped}/{len(universe)} 标的（窗口含未解释漏采），"
                    f"universe 收窄——疑 lake 漏采，查 scan/repair 闭环"
                )
```

`_CONTINUITY_FILTER_ALERT_FLOOR` 加到 engine 模块级常量区（注释 Why：节流阈值，防全市场偶发过滤刷屏；单标的漏采只 warning 不告警）。

- [ ] **Step 5: 跑测试确认通过**

Run: `.venv310/Scripts/python.exe -m pytest tests/trading/test_engine*.py -k "continuity or _eod" -v`
Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add trading/engine.py tests/trading/
git commit -m "feat(trading): filter_universe 过滤超阈值接 _alert_critical 告警——② 残数据有声（W0/D1）"
```

---

### Task 8：全量回归 + 文档订正 + W0 收尾（组 B · D1）

**Files:** 无代码改动（验证 + 文档）。

- [ ] **Step 1: 全量回归**

Run: `.venv310/Scripts/python.exe -m pytest tests/ -q`
Expected: 全绿（基线 ~1684 + 本计划净增测试）。重点确认：`tests/trading/`、`tests/experiment/`、`tests/test_sync_cli.py`、`tests/test_integrity.py`、`tests/test_sync_macro_credit.py` 全绿。

- [ ] **Step 2: 文档订正 `docs/architecture/06-tech-debt.md`**

更新对应行（spec §0.4 + 本计划闭合项）：
- H3 Phase C 收尾：C2d 下沉 + JSON 窗口关闭 → 标「✅ 已治理（2026-08-12）」。
- D1 T13-B 收尾：3 处裸写接入 + P1 合并语义 + P2/P3 测试 + 过滤告警 → 标「✅ 已治理」。

- [ ] **Step 3: Commit**

```bash
git add docs/architecture/06-tech-debt.md
git commit -m "docs(tech-debt): W0 收尾闭合——H3 C2d/JSON窗口 + D1 裸写/P1/P2/P3/告警 全治理"
```

---

## Self-Review

**1. Spec 覆盖**：
- ✅ H3 C2d（spec §2.5 第 1 项）→ Task A1。
- ✅ H3 JSON 回退窗口关闭（spec §2.5 第 2 项 + DG-5）→ Task A2。
- ✅ H3 _resolve_account_id 收口（spec §2.5 第 3 项）→ 已闭合（commit `7fbb68b8`，本计划不含）。
- ✅ D1 3 处裸写（spec §3.3.3）→ Task B1（sync_macro）+ B2（sync_data_lake×2）。
- ✅ D1 P1 shibor/宏观写误伤（spec §3.3.4）→ Task B1（合并语义彻底治）。
- ✅ D1 P2 select_keys（spec §3.3.4）→ Task B3（已修补回归测试）。
- ✅ D1 P3 双轨残留（spec §3.3.4）→ Task B4（保留标记 + 守卫）。
- ✅ D1 ②告警通道（spec §3.3.5）→ Task B5。
- ✅ D1 降级语义（DG-7）→ 已裁决不推翻，无代码动作。

**2. Placeholder 扫描**：B5 Step 2 的失败测试用了 `...` 占位（因 _eod 测试 setup 重，需 Step 1 定位后据实写）——这是「先定位再写测试」的诚实标注，非偷懒占位；执行者据 Step 1 grep 结果 + 同文件既有 _eod 测试范式补全。其余每步含实际代码/命令。A2 Step 1 的分叉判定是核对步骤（有确切 grep 命令 + 判定逻辑），非 placeholder。

**3. 类型/命名一致**：`report_plan_attribution(since)` 在 A1 定义 + 测试 + CLI 一致；`safe_overwrite(path, df)` 签名与 `data/integrity.py:241` 一致；`list_signals_with_meta_by_plan_date_range(since=, until=)` 关键字与 `state_store.py:1136` 一致；`_alert_critical(msg)` 与 `trading/critical.py:40` 一致。

**4. 行为等价红线**：A2 load_plan 关 fallback 后消费方 None 处理已存在（pre_open 安全跳过）；B1 sync_macro 合并在完整区间输入下与旧覆盖逐字一致（concat 去重 keep last，完整区间 new==existing 范围时结果不变）；B2 首次写 safe_overwrite 放行不破坏。均有对应测试守护。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-08-12-w0-tail.md`. Two execution options:

1. **Subagent-Driven (recommended)** — 每个 Task 派 fresh subagent，Task 间 review。A2 风险较高（load_plan 19 调用者），Subagent 隔离 + 两阶段 review 更稳。
2. **Inline Execution** — 本 session 用 executing-plans 批量执行 + 检查点。B1-B4 是无争议小改，Inline 顺畅；A1/A2/B5 有设计含量，建议 checkpoint。

哪种？（W0 收尾后，紧接着进入 W1-A t2-keystone 计划编写。）
