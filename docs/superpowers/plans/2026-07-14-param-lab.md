# Parameter Lab (/lab) 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现。步骤用 checkbox（`- [ ]`）跟踪。

**Goal:** 新增独立 `/lab` 参数实验室前端，消费 Spec 1 异步回测任务 API（SQLite），提供「配参→提交异步回测→看进度→审阅结果→微调重跑」工作台，并下线 `/caisen` 老「历史回放」tab。

**Architecture:** 纵向堆叠画布（参数详情/收益走势→买卖日志→任务列表 master-detail）+「新建回测」抽屉；后端仅补 `DELETE /replay/tasks/{id}` 一个端点；从 `/caisen` 抽取 `ReplayReportPanel.vue` 复用；轮询观测进度（无 SSE）；AI 建议为任务列表占位列留 Spec 3。

**Tech Stack:** Vue 3.5 + Element Plus 2.9 + vue-echarts 5 + vue-router 4（前端）；FastAPI + sqlite3（后端，仅 1 端点）；vitest + @vue/test-utils（前端单测）；pytest + FastAPI TestClient（后端单测）；Playwright + with_server.py（E2E）。

## Global Constraints

- **全中文**：所有对话/文档/代码注释 100% 中文；注释讲清 What + Why（交易物理意图/边界）。
- **design token**：前端新增 CSS 一律 `var(--qt-*)` + utils.css 工具类（`.qt-card`/`.qt-view-shell`/`.qt-section-title`/`.qt-empty`），**禁裸 hex**；A 股红涨绿跌用 `--qt-up`(#ef5350)/`--qt-down`(#26a69a)；例外：ECharts option 内色值（canvas 不解析 CSS var）、`rgba()`、纯黑白。
- **极简/无黑盒**：不引新依赖；扁平结构，无深继承；数学/数据清洗显式直白。
- **Python 解释器**：后端测试与 E2E 用 `.venv310` 绝对路径 python（`web/` 同级执行）。
- **前端校验**：`cd web && npm run typecheck`（vue-tsc）+ `npm run test`（vitest run）。
- **后端校验**：`.venv310/Scripts/python.exe -m pytest tests/test_caisen_api.py -q`（全量 `-m pytest -q` 应保持 700 绿）。
- **边界审查**：网络抖动/404/422/FAILED/取消竞态/worker 崩溃/空态/NaN 全部显式处置（见 spec §11）。
- **TDD + 频繁提交**：每个任务结束 commit；commit message 末尾加 `Co-Authored-By: Claude <noreply@anthropic.com>`。

---

## File Structure

| 文件 | 责任 | 任务 |
|---|---|---|
| `server/api/v1/caisen.py` | 加 `DELETE /replay/tasks/{id}` 薄路由 | T1 |
| `tests/test_caisen_api.py` | DELETE 端点契约测试 | T1 |
| `web/src/api/caisen.ts` | 加异步任务 5 函数 + 类型（T2）；删老同步死函数（T8） | T2,T8 |
| `web/src/api/caisen.spec.ts` | 异步函数契约测试（T2）；移除老函数用例（T8） | T2,T8 |
| `web/src/components/lab/paramMeta.ts` | 中文标题映射 + 7 分组（详情面板与抽屉共用） | T3 |
| `tests/test_param_meta_sync.py` | 跨层同步守护：config 字段集 ↔ PARAM_META 键集双向匹配 | T3 |
| `web/src/components/lab/ReplayReportPanel.vue` | 结果渲染（equity+统计卡+形态+月度+流水），从 /caisen 抽取 | T4 |
| `web/src/components/lab/NewReplayDrawer.vue` | 新建回测抽屉：分组参数表单 + 区间/标的 + 提交 | T5 |
| `web/src/views/ParamLabView.vue` | 顶层视图：4 区画布 + master-detail + 轮询 + 抽屉编排 | T6 |
| `web/src/router/index.ts` | `/lab` 路由 | T6 |
| `web/src/App.vue` | 导航项「参数实验室」 | T6 |
| `tests/e2e/lab_param_lab.py` | /lab 端到端 E2E | T7 |
| `web/src/views/CaisenScreenView.vue` | 下线回放 tab + 复用 ReplayReportPanel（T4 引用；T8 删 tab） | T4,T8 |
| `tests/e2e/caisen_replay_tab.py` | 改造为「回放 tab 已移除 + 扫描链路正常」回归守护 | T8 |

---

## Task 1: 后端 DELETE /replay/tasks/{id} 端点

**Files:**
- Modify: `server/api/v1/caisen.py`（在 `cancel_replay_task` 后追加）
- Test: `tests/test_caisen_api.py`（在异步端点测试类内追加，复用 `_isolate_db`）

**Interfaces:**
- Consumes: `caisen.replay_tasks_db.delete_task(task_id) -> bool`（已存在，replay_tasks_db.py:205）
- Produces: `DELETE /api/v1/caisen/replay/tasks/{task_id}` → `200 {"ok": true}` / `404`

- [ ] **Step 1: 写失败测试**

在 `tests/test_caisen_api.py` 的异步端点测试类（含 `test_cancel_sets_abort_flag_when_scheduler_attached` 的那个类）末尾追加：

```python
    def test_delete_replay_task_200_and_404(self, client, tmp_path, monkeypatch):
        """DELETE /replay/tasks/{id} → 200 {ok:true}；不存在 → 404。

        物理意图：任务历史清理能力（spec §5 交互6）。后端薄路由不加状态守卫——
        「不删 RUNNING」是前端 UX 约定（RUNNING 行无删除按钮），非后端硬约束。
        """
        self._isolate_db(tmp_path, monkeypatch)
        from caisen import replay_tasks_db
        tid = replay_tasks_db.create_task(
            {"start": "s", "end": "e", "universe": None, "cfg_override": {}})

        # 存在 → 200 + {ok:true}，且 DB 行确实删除
        resp = client.delete(f"/api/v1/caisen/replay/tasks/{tid}")
        assert resp.status_code == 200, resp.text
        assert resp.json() == {"ok": True}
        assert replay_tasks_db.get_task(tid) is None

        # 不存在 → 404（与 get 同源契约，状态机不进 NULL）
        resp2 = client.delete("/api/v1/caisen/replay/tasks/nope-id")
        assert resp2.status_code == 404
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv310/Scripts/python.exe -m pytest tests/test_caisen_api.py -k delete_replay_task -q`
Expected: FAIL（404/405——路由未定义）

- [ ] **Step 3: 加薄路由**

在 `server/api/v1/caisen.py` 的 `cancel_replay_task` 之后追加：

```python
@router.delete("/replay/tasks/{task_id}", summary="删除异步任务记录")
def delete_replay_task(task_id: str) -> Dict[str, bool]:
    """删除单条异步任务（清理历史）。

    物理意图（spec §5 交互6 清理能力）：任务历史不能只增不删。「不删 RUNNING」是
    前端 UX 约定（RUNNING 行只显示「取消」），后端薄路由不加状态守卫——delete_task
    直删任意行。RUNNING 行被直删时 worker 仍会跑完但 mark_success/update 进 0 行（无害），
    故由前端按钮可见性兜底即可，YAGNI 不加后端守卫。

    异常策略：task_id 不存在 → delete_task 返 False → 本层转 404（与 get 同源契约）。
    """
    ok = replay_tasks_db.delete_task(task_id)
    if not ok:
        raise HTTPException(status_code=404, detail=f"任务不存在：task_id={task_id!r}")
    return {"ok": True}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv310/Scripts/python.exe -m pytest tests/test_caisen_api.py -k "replay_task or replay_async" -q`
Expected: PASS（新测试 + 既有异步端点测试全绿）

- [ ] **Step 5: 提交**

```bash
git add server/api/v1/caisen.py tests/test_caisen_api.py
git commit -m "feat(server): DELETE /replay/tasks/{id} 异步任务清理端点（Spec 2 Task 1）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 2: 前端 API client 异步任务函数 + 类型

**Files:**
- Modify: `web/src/api/caisen.ts`（追加类型 + 5 函数；**不删**老函数，留 T8）
- Test: `web/src/api/caisen.spec.ts`（mock 工厂补 `delete` + 5 个新用例）

**Interfaces:**
- Consumes: `./client` 的 `apiClient`（单例，已有）
- Produces: `submitReplayAsync` / `listReplayTasks` / `getReplayTask` / `cancelReplayTask` / `deleteReplayTask` + 类型 `ReplayTask` / `ReplayTaskDetail` / `ReplayAsyncRequestBody` / `CancelResponse`

- [ ] **Step 1: 写失败测试**

`web/src/api/caisen.spec.ts` 顶部 `vi.mock('./client', ...)` 工厂补 `delete: vi.fn()`：

```ts
vi.mock('./client', () => ({
  apiClient: {
    post: vi.fn(),
    get: vi.fn(),
    patch: vi.fn(),
    delete: vi.fn(),   // 新增：DELETE /replay/tasks/{id} 用
  },
}))
```

`mockReset` / `mockResolvedValue` 块补 `mockDelete`：

```ts
const mockDelete = vi.mocked(apiClient.delete)
// beforeEach 内：
mockDelete.mockReset()
mockDelete.mockResolvedValue({} as any)
```

import 行补新函数：

```ts
import {
  scan, listPlans, getPlan, reviewPlan, activatePlan, getChart, runReplay,
  submitReplayAsync, listReplayTasks, getReplayTask, cancelReplayTask, deleteReplayTask,
} from './caisen'
```

在 `describe` 块末尾追加 5 个用例：

```ts
  it('submitReplayAsync: POST /replay/async，body 透传，timeout 10000', async () => {
    await submitReplayAsync({ start: '2024-01-01', end: '2024-06-01', cfg_override: { min_rr_ratio: 1.5 } })
    expect(mockPost).toHaveBeenCalledWith(
      '/api/v1/caisen/replay/async',
      { start: '2024-01-01', end: '2024-06-01', cfg_override: { min_rr_ratio: 1.5 } },
      { timeout: 10000 },
    )
  })

  it('listReplayTasks() 无 status：GET /replay/tasks，params 空', async () => {
    await listReplayTasks()
    expect(mockGet).toHaveBeenCalledWith('/api/v1/caisen/replay/tasks', { params: {}, timeout: 10000 })
  })

  it('listReplayTasks(status)：params 含 status', async () => {
    await listReplayTasks('RUNNING')
    expect(mockGet).toHaveBeenCalledWith(
      '/api/v1/caisen/replay/tasks', { params: { status: 'RUNNING' }, timeout: 10000 })
  })

  it('getReplayTask：task_id 经 encodeURIComponent', async () => {
    await getReplayTask('abc 123')
    expect(mockGet).toHaveBeenCalledWith('/api/v1/caisen/replay/tasks/abc%20123', { timeout: 10000 })
  })

  it('cancelReplayTask：POST .../cancel，空 body {}', async () => {
    await cancelReplayTask('t1')
    expect(mockPost).toHaveBeenCalledWith('/api/v1/caisen/replay/tasks/t1/cancel', {}, { timeout: 10000 })
  })

  it('deleteReplayTask：DELETE .../tasks/{id}', async () => {
    await deleteReplayTask('t1')
    expect(mockDelete).toHaveBeenCalledWith('/api/v1/caisen/replay/tasks/t1', { timeout: 10000 })
  })
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd web && npm run test -- caisen.spec`
Expected: FAIL（新函数未定义）

- [ ] **Step 3: 加类型 + 函数**

`web/src/api/caisen.ts` 在「请求体类型」段后追加类型：

```ts
// ============ 异步回测任务类型（Spec 1 SQLite 任务表，Spec 2 /lab 消费） ============

/** 异步任务状态机。对齐 server.schemas.caisen.ReplayTaskSummary.status。 */
export type ReplayTaskStatus = 'PENDING' | 'RUNNING' | 'SUCCESS' | 'FAILED' | 'CANCELLED'

/** GET /caisen/replay/tasks 列表项（对齐 ReplayTaskSummary）。 */
export interface ReplayTask {
  task_id: string
  created_at: string
  status: ReplayTaskStatus
  progress: number                                // 0-100（已处理 symbol 占比）
  start?: string | null
  end?: string | null
  universe_n?: number | null                      // -1 = 全市场
  cfg_override?: Record<string, unknown>
}

/** GET /caisen/replay/tasks/{id} 详情（对齐 ReplayTaskDetail）。
 *  SUCCESS 时 report 内嵌完整 ReplayReport（走势/统计/日志一次取齐）。 */
export interface ReplayTaskDetail extends ReplayTask {
  report?: ReplayReport | null
  error?: string | null
  started_at?: string | null
  finished_at?: string | null
  last_heartbeat?: string | null
}

/** POST /caisen/replay/async 请求体（对齐 ReplayAsyncRequest，无 save——异步落 SQLite 是本职）。 */
export interface ReplayAsyncRequestBody {
  start: string
  end: string
  universe?: string[] | null                      // null/缺省 = 全市场
  cfg_override?: Record<string, unknown>
}

/** POST /caisen/replay/tasks/{id}/cancel 响应（对齐 CancelResponse）。 */
export interface CancelResponse {
  task_id: string
  cancelled: boolean
  message: string
}
```

在文件末尾「回测历史记录 API」段前追加异步任务 API 段：

```ts
// ============ 异步回测任务 API（Spec 1：SQLite 任务表，/lab 消费） ============

/** POST /caisen/replay/async：提交异步回测（立即返 task_id，不阻塞）。 */
export function submitReplayAsync(body: ReplayAsyncRequestBody): Promise<{ task_id: string }> {
  return apiClient.post('/api/v1/caisen/replay/async', body, { timeout: 10000 })
}

/** GET /caisen/replay/tasks：异步任务列表（降序，可按 status 过滤）。 */
export function listReplayTasks(status?: ReplayTaskStatus): Promise<ReplayTask[]> {
  return apiClient.get('/api/v1/caisen/replay/tasks', { params: status ? { status } : {}, timeout: 10000 })
}

/** GET /caisen/replay/tasks/{id}：单任务详情（status/progress/report/error/时间戳）。 */
export function getReplayTask(taskId: string): Promise<ReplayTaskDetail> {
  return apiClient.get(`/api/v1/caisen/replay/tasks/${encodeURIComponent(taskId)}`, { timeout: 10000 })
}

/** POST /caisen/replay/tasks/{id}/cancel：取消（置 abort flag，轮询到 CANCELLED 生效）。 */
export function cancelReplayTask(taskId: string): Promise<CancelResponse> {
  return apiClient.post(`/api/v1/caisen/replay/tasks/${encodeURIComponent(taskId)}/cancel`, {}, { timeout: 10000 })
}

/** DELETE /caisen/replay/tasks/{id}：删除任务记录（清理历史；前端仅对非 RUNNING 行调用）。 */
export function deleteReplayTask(taskId: string): Promise<{ ok: boolean }> {
  return apiClient.delete(`/api/v1/caisen/replay/tasks/${encodeURIComponent(taskId)}`, { timeout: 10000 })
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd web && npm run test -- caisen.spec && npm run typecheck`
Expected: PASS（新 5 用例 + 既有用例全绿；typecheck 无错）

- [ ] **Step 5: 提交**

```bash
git add web/src/api/caisen.ts web/src/api/caisen.spec.ts
git commit -m "feat(web/api): 异步回测任务 client 5 函数+类型（Spec 2 Task 2）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 3: 中文参数映射 paramMeta.ts + 跨层同步守护

**Files:**
- Create: `web/src/components/lab/paramMeta.ts`
- Test: `tests/test_param_meta_sync.py`

**Interfaces:**
- Consumes: `caisen.config.StrategyConfig` 字段集（同步守护用）
- Produces: `PARAM_GROUPS: ParamGroup[]`、`PARAM_META: Record<string, {title, group}>`（T5 抽屉、T6 详情面板共用）

- [ ] **Step 1: 写失败测试（跨层同步守护）**

Create `tests/test_param_meta_sync.py`：

```python
# -*- coding: utf-8 -*-
"""paramMeta.ts 中文映射 ↔ StrategyConfig 字段集 跨层同步守护（Spec 2 Task 3）。

物理意图：paramMeta.ts 的 PARAM_META 是前端「中文标题+分组」单一真相源，必须覆盖
StrategyConfig 全部字段，否则 /lab 参数面板/抽屉会漏字段或留英文键。本测试读 paramMeta.ts
文本正则抽键，与 StrategyConfig.model_fields 双向比对——config.py 加字段时自动失败，
强制 paramMeta.ts 同步补条目（防漂移）。
"""
import re
from pathlib import Path

from caisen.config import StrategyConfig

PARAM_META_TS = Path(__file__).resolve().parents[1] / "web" / "src" / "components" / "lab" / "paramMeta.ts"


def _extract_param_meta_keys() -> set[str]:
    """从 paramMeta.ts 文本正则抽取 PARAM_META 的键（形如 `field_name: {`）。"""
    text = PARAM_META_TS.read_text(encoding="utf-8")
    # 匹配 PARAM_META 对象体内的 `  name: {` 行（缩进 + 合法标识符 + 冒号 + 空格 + {）
    block = text.split("PARAM_META", 1)[1]
    # 截到 PARAM_META 对象闭合（首个仅含 `}` 的行）
    block = block.split("}\n", 1)[0]
    return set(re.findall(r"^\s{2}([A-Za-z_][A-Za-z0-9_]*):\s*\{", block, re.MULTILINE))


def test_param_meta_covers_all_strategy_fields():
    """PARAM_META 键集 == StrategyConfig 字段集（双向匹配，防漏/防孤儿）。"""
    assert PARAM_META_TS.exists(), f"paramMeta.ts 不存在：{PARAM_META_TS}"
    cfg_fields = set(StrategyConfig.model_fields.keys())
    meta_keys = _extract_param_meta_keys()
    missing = cfg_fields - meta_keys          # config 有、paramMeta 漏（漏字段→面板缺项）
    orphan = meta_keys - cfg_fields           # paramMeta 有、config 无（拼写错/已删字段）
    assert not missing, f"paramMeta.ts 漏字段（补中文标题）：{sorted(missing)}"
    assert not orphan, f"paramMeta.ts 孤儿键（拼写错或字段已删）：{sorted(orphan)}"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv310/Scripts/python.exe -m pytest tests/test_param_meta_sync.py -q`
Expected: FAIL（paramMeta.ts 不存在）

- [ ] **Step 3: 写 paramMeta.ts**

Create `web/src/components/lab/paramMeta.ts`（中文标题映射 + 7 分组，键集须与 `caisen/config.py` 的 `StrategyConfig` 字段一一对应）：

```ts
/**
 * 蔡森策略参数中文映射 + 分组（/lab 参数详情面板与新建回测抽屉共用）。
 *
 * 物理定位（CLAUDE.md 极简）：config.py 的 Field description 已中文但过长（含 rationale），
 * 不适合做表单 label。本文件用扁平 dict 提供「短中文标题 + 分组」，与 config 字段名同源——
 * 前后端参数名仍由 GET /config/schema 的 properties 键反射，杜绝漂移。
 *
 * 同步守护：tests/test_param_meta_sync.py 断言本文件键集 == StrategyConfig.model_fields，
 * config.py 加字段时该测试强制失败 → 此处同步补条目。
 *
 * 分组对齐 config.py 的 7 大物理意图分组：时间跨度/空间高度/量价配合/交易执行/时间止损/风控/蔡森方法学。
 */
export type ParamGroup =
  | '时间跨度' | '空间高度' | '量价配合' | '交易执行'
  | '时间止损' | '风控' | '蔡森方法学'

/** 分组展示顺序（详情面板/抽屉按此序折叠渲染）。 */
export const PARAM_GROUPS: ParamGroup[] = [
  '时间跨度', '空间高度', '量价配合', '交易执行',
  '时间止损', '风控', '蔡森方法学',
]

export interface ParamMeta {
  title: string           // 短中文标题（表单 label / 详情字段名）
  group: ParamGroup       // 归属分组
}

/** 字段名 → {标题, 分组}。键集须与 caisen.config.StrategyConfig.model_fields 完全一致。 */
export const PARAM_META: Record<string, ParamMeta> = {
  // —— 时间跨度 ——
  min_pattern_bars:   { title: '形态最小跨度',         group: '时间跨度' },
  max_pattern_bars:   { title: '形态最大跨度',         group: '时间跨度' },
  symmetry_tolerance: { title: '左右时间对称容忍度',   group: '时间跨度' },
  // —— 空间高度 ——
  zigzag_threshold_atr: { title: 'ZigZag波段阈值(ATR倍)', group: '空间高度' },
  min_pattern_depth:    { title: '形态最浅幅度',          group: '空间高度' },
  max_pattern_depth:    { title: 'W底最深幅度阈值',       group: '空间高度' },
  hs_max_pattern_depth: { title: '头肩底深度宽阈值',      group: '空间高度' },
  w_price_tolerance:    { title: 'W底两底价格容忍度',     group: '空间高度' },
  // —— 量价配合 ——
  right_vol_shrink:        { title: '右底缩量比例上限', group: '量价配合' },
  breakout_vol_multiplier: { title: '突破放量倍数',     group: '量价配合' },
  // —— 交易执行 ——
  pullback_window_bars: { title: '回踩触发窗口(K线)', group: '交易执行' },
  pullback_max_pct:     { title: '回踩最高价容忍%',   group: '交易执行' },
  stop_loss_atr_buffer: { title: '止损ATR缓冲',       group: '交易执行' },
  min_rr_ratio:         { title: '盈亏比下限',         group: '交易执行' },
  // —— 时间止损 ——
  max_holding_bars:         { title: '最大持仓周期',    group: '时间止损' },
  timeout_exit_threshold:   { title: '超时砍亏浮盈阈值', group: '时间止损' },
  trailing_activation_bars: { title: '移动止盈激活天数', group: '时间止损' },
  trailing_to_breakeven:    { title: '移动止盈锁本金',   group: '时间止损' },
  // —— 风控 ——
  liquidity_min_amount: { title: '流动性成交额下限',   group: '风控' },
  hv_window:            { title: '历史波动率窗口',     group: '风控' },
  hv_max_quantile:      { title: 'HV异常分位上限',     group: '风控' },
  max_position_pct:     { title: '单标的占总资金上限', group: '风控' },
  macro_regime_veto:    { title: '宏观收缩期一票否决', group: '风控' },
  confirm_bars:         { title: 'ZigZag末尾pivot确认窗', group: '风控' },
  // —— 蔡森方法学 ——
  neckline_height_multiple:   { title: '颈线满足级数n',   group: '蔡森方法学' },
  abc_wave_detect:            { title: 'ABC波过程识别',   group: '蔡森方法学' },
  right_above_left:           { title: '右脚>左脚硬规则', group: '蔡森方法学' },
  ma26w_filter:               { title: '26周线打底过滤',  group: '蔡森方法学' },
  ma26w_window:               { title: '26周线计算窗口',  group: '蔡森方法学' },
  pattern_tension_ratio:      { title: '幅宽张力比例下限', group: '蔡森方法学' },
  pattern_width_bonus:        { title: '幅宽加分',         group: '蔡森方法学' },
  enable_pot_breakout:        { title: '启用破头锅形态',   group: '蔡森方法学' },
  enable_bottom_flip:         { title: '启用破底翻形态',   group: '蔡森方法学' },
  enable_triangle_bottom:     { title: '启用收敛三角底',   group: '蔡森方法学' },
  triangle_max_pattern_depth: { title: '三角边长比上限',   group: '蔡森方法学' },
  triangle_breakout_min:      { title: '三角突破进度下限', group: '蔡森方法学' },
  triangle_breakout_max:      { title: '三角突破进度上限', group: '蔡森方法学' },
  false_breakout_threshold:   { title: '假突破跌破阈值',   group: '蔡森方法学' },
  false_breakout_window:      { title: '假突破判定窗口',   group: '蔡森方法学' },
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv310/Scripts/python.exe -m pytest tests/test_param_meta_sync.py -q`
Expected: PASS（键集与 StrategyConfig 双向匹配）

如失败：按报错补/删 PARAM_META 条目，使键集与 `StrategyConfig.model_fields` 完全一致。

- [ ] **Step 5: 提交**

```bash
git add web/src/components/lab/paramMeta.ts tests/test_param_meta_sync.py
git commit -m "feat(lab): paramMeta 中文参数映射+跨层同步守护（Spec 2 Task 3）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 4: 抽取 ReplayReportPanel.vue（/caisen 复用，行为不变）

**Files:**
- Create: `web/src/components/lab/ReplayReportPanel.vue`
- Modify: `web/src/views/CaisenScreenView.vue`（回放 tab 内联渲染 → 引用组件）

**Interfaces:**
- Consumes: `ReplayReport`（caisen.ts 既有类型）、ECharts（`echarts-terminal-dark.ts` 主题）、`--qt-*` token
- Produces: `<ReplayReportPanel :report="report" />`——纯展示组件，props.report 驱动 equity/统计卡/形态/月度/流水

**说明（行为保持的重构）**：`CaisenScreenView.vue` 现有回放结果渲染（统计卡 + equity ECharts + 形态分布 + 月度收益 + 买卖流水表，约 line 515-545 的 computed 与 line 875+ 的模板）整体迁入新组件，ref `replayReport` 改为 props `report`。TDD 验证 = 既有 E2E `caisen_replay_tab.py` 仍绿（它断言「买卖流水」section 渲染）。

- [ ] **Step 1: 创建 ReplayReportPanel.vue**

将 `CaisenScreenView.vue` 中回放结果的模板 + computed（`monthlySorted` / `patternDist` / `equityOption` 等）迁入新组件，所有 `replayReport.value` 改为 props `report`。组件骨架：

```vue
<script setup lang="ts">
/**
 * 回放结果渲染面板（/lab 与 /caisen 复用）。
 *
 * 物理定位：纯展示组件——给定 ReplayReport，渲染 equity 曲线（ECharts）+ 统计卡
 * （命中/胜率/盈亏比/回撤/年化/平均持仓）+ 形态分布 + 月度收益 + 买卖流水表。
 * 从 CaisenScreenView 抽取（Spec 2 Task 4），零业务逻辑，props 单向驱动。
 *
 * 样式约束：用 --qt-* token；A 股红涨绿跌 --qt-up/--qt-down；ECharts option 内色值裸 hex 例外。
 */
import { computed } from 'vue'
import VChart from 'vue-echarts'
import { use } from 'echarts/core'
import { CanvasRenderer } from 'echarts/renderers'
import { LineChart } from 'echarts/charts'
import { GridComponent, TooltipComponent, LegendComponent } from 'echarts/components'
import { init as initTheme } from '@/theme/echarts-terminal-dark'
import type { ReplayReport } from '@/api/caisen'

use([CanvasRenderer, LineChart, GridComponent, TooltipComponent, LegendComponent])
initTheme()

const props = defineProps<{ report: ReplayReport }>()

// 月度收益按月份时间序（柱状/折线展示用）——从 CaisenScreenView 同名 computed 迁入
const monthlySorted = computed(() =>
  Object.entries(props.report.monthly_returns).sort(([a], [b]) => a.localeCompare(b)),
)
// 形态分布（回放报告里的形态命中数）
const patternDist = computed(() => Object.entries(props.report.pattern_dist))
// equity 曲线 option——从 CaisenScreenView equityOption computed 迁入，数据源改 props.report.equity_curve
const equityOption = computed(() => {
  const curve = props.report.equity_curve || []
  const ann = ((props.report.annualized_return || 0) * 100).toFixed(2)
  return {
    // …（原 equityOption 内容：xAxis date / yAxis equity / series line / 标题含年化）
    // 注：色值用裸 hex（ECharts canvas 不解析 CSS var，既定例外）
    title: { text: `资金曲线 · 年化 ${ann}%`, textStyle: { color: '#c8cdd0' } },
    xAxis: { type: 'category', data: curve.map((p) => p.date) },
    yAxis: { type: 'value', scale: true },
    series: [{ type: 'line', data: curve.map((p) => p.equity), smooth: true, symbol: 'none', lineStyle: { color: '#26a69a' } }],
    tooltip: { trigger: 'axis' },
  }
})
</script>

<template>
  <div class="replay-report">
    <!-- 统计卡：命中/胜率/盈亏比/回撤/年化/平均持仓（从 CaisenScreenView .mv 卡片块迁入） -->
    <!-- equity 曲线：<v-chart :option="equityOption" autoresize /> -->
    <!-- 形态分布 + 月度收益（monthlySorted / patternDist） -->
    <!-- 买卖流水表：report.trades（列：标的/形态/买入日价/卖出日价/离场原因/盈亏比/持仓天） -->
  </div>
</template>
```

> 实现要点：打开 `CaisenScreenView.vue`，把回放结果区（`v-if="replayReport"` 的 `.replay-report` 块及其专属 style、`equityOption`/`monthlySorted`/`patternDist` computed）**逐字迁入**本组件模板/脚本，`replayReport.value` → `props.report`，`replayReport` → `props.report`。ECharts 按现有 CaisenScreenView 的 `use([...])` 注册方式对齐（若原文件用全量 `echarts` 则保持一致）。

- [ ] **Step 2: CaisenScreenView 改为引用组件**

`web/src/views/CaisenScreenView.vue`：
- 顶部 import：`import ReplayReportPanel from '@/components/lab/ReplayReportPanel.vue'`
- 回放 tab 内原 `.replay-report` 整块替换为：
  ```vue
  <ReplayReportPanel v-if="replayReport" :report="replayReport" />
  ```
- 删除已迁入的 computed（`equityOption`/`monthlySorted`/`patternDist`）与回放结果专属 style（迁入组件 `<style scoped>`）。`replayReport` ref 本身保留（回放 tab 仍用老同步链路赋值，T8 才删 tab）。

- [ ] **Step 3: 校验——typecheck + 单测 + 既有 E2E 绿**

Run:
```bash
cd web && npm run typecheck && npm run test
QUANTER_API_TOKEN=e2e-token .venv310/Scripts/python.exe "$WITH_SERVER" \
  --server ".venv310/Scripts/python.exe -m uvicorn server.main:app --port 8000" --port 8000 \
  --server "npm --prefix web run dev" --port 5173 --timeout 120 \
  -- ".venv310/Scripts/python.exe" tests/e2e/caisen_replay_tab.py
```
（`$WITH_SERVER` = webapp-testing skill 的 `scripts/with_server.py` 绝对路径）
Expected: typecheck 无错；vitest 全绿；E2E 输出 `[PASS] replay tab UI 链路通`（买卖流水 section 仍由 ReplayReportPanel 渲染）。

- [ ] **Step 4: 提交**

```bash
git add web/src/components/lab/ReplayReportPanel.vue web/src/views/CaisenScreenView.vue
git commit -m "refactor(lab): 抽取 ReplayReportPanel 供 /caisen 复用（Spec 2 Task 4）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 5: NewReplayDrawer.vue（新建回测抽屉）

**Files:**
- Create: `web/src/components/lab/NewReplayDrawer.vue`
- Test: `web/src/components/lab/NewReplayDrawer.spec.ts`

**Interfaces:**
- Consumes: `paramMeta.ts`（PARAM_META/PARAM_GROUPS）、`ReplayAsyncRequestBody` 类型
- Props: `visible: boolean`（v-model）、`configSchema: Record<string, any>`、`prefill?: Record<string, unknown> | null`、`submitting?: boolean`
- Emits: `update:visible`、`submit`（payload: `ReplayAsyncRequestBody`）

- [ ] **Step 1: 写失败测试**

Create `web/src/components/lab/NewReplayDrawer.spec.ts`：

```ts
/**
 * NewReplayDrawer 组件单测：分组表单渲染 + prefill + 提交 body 契约。
 * 复用 DatasetTable.spec.ts 的 jsdom polyfill + ElementPlus 全量注册模式。
 */
import { describe, it, expect, vi } from 'vitest'
import { mount, flushPromises } from '@vue/test-utils'
import ElementPlus from 'element-plus'
import NewReplayDrawer from './NewReplayDrawer.vue'
import { PARAM_META } from './paramMeta'

class MockObserver { observe() {} unobserve() {} disconnect() {} takeRecords() { return [] } }
;(globalThis as any).ResizeObserver = MockObserver
;(globalThis as any).IntersectionObserver = MockObserver
;(globalThis as any).matchMedia = (globalThis as any).matchMedia || ((q: string) => ({
  matches: false, media: q, onchange: null, addListener: vi.fn(), removeListener: vi.fn(),
  addEventListener: vi.fn(), removeEventListener: vi.fn(), dispatchEvent: vi.fn(),
}))

// 最小 schema fixture：properties 含两个已知字段（min_rr_ratio + max_holding_bars）
const SCHEMA = {
  properties: {
    min_rr_ratio:     { type: 'number', default: 1.5, description: '盈亏比下限' },
    max_holding_bars: { type: 'integer', default: 15, description: '最大持仓周期' },
  },
}

describe('NewReplayDrawer', () => {
  it('visible 时渲染 7 个分组标题 + 区间/标的输入', async () => {
    const wrapper = mount(NewReplayDrawer, {
      props: { visible: true, configSchema: SCHEMA, prefill: null },
      global: { plugins: [ElementPlus] },
    })
    await flushPromises()
    // 7 分组（PARAM_GROUPS）至少渲染「交易执行」「时间止损」（含两字段）
    expect(wrapper.text()).toContain('交易执行')
    expect(wrapper.text()).toContain('时间止损')
    expect(wrapper.find('input[placeholder*="开始"]').exists() || wrapper.text()).toBeTruthy()
  })

  it('prefill 灌入：min_rr_ratio 显示 prefill 值而非 schema 默认', async () => {
    const wrapper = mount(NewReplayDrawer, {
      props: { visible: true, configSchema: SCHEMA, prefill: { min_rr_ratio: 2.0 } },
      global: { plugins: [ElementPlus] },
    })
    await flushPromises()
    // el-input-number 的输入框值 = 2.0（prefill 覆盖默认 1.5）
    const input = wrapper.find('input[role="spinbutton"]')
    expect((input.element as HTMLInputElement).value).toContain('2')
  })

  it('点提交 emit submit，payload 含 start/end/universe/cfg_override（含 prefill 改值）', async () => {
    const wrapper = mount(NewReplayDrawer, {
      props: { visible: true, configSchema: SCHEMA, prefill: { min_rr_ratio: 2.0 } },
      global: { plugins: [ElementPlus] },
    })
    await flushPromises()
    await wrapper.get('button[data-testid="submit-replay"]').trigger('click')
    const evt = wrapper.emitted('submit')
    expect(evt).toBeTruthy()
    const body = evt![0][0] as any
    expect(body).toHaveProperty('start')
    expect(body).toHaveProperty('end')
    expect(body).toHaveProperty('cfg_override')
    expect(body.cfg_override.min_rr_ratio).toBe(2.0)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd web && npm run test -- NewReplayDrawer`
Expected: FAIL（组件不存在）

- [ ] **Step 3: 实现 NewReplayDrawer.vue**

```vue
<script setup lang="ts">
/**
 * 新建回测抽屉（/lab「＋新建回测」入口）。
 *
 * 物理定位：可编辑参数表单（configSchema 反射 + paramMeta 中文分组）+ 区间/标的 +
 * 提交。默认从 prefill（当前选中任务 cfg）灌入，便于微调重跑。提交 emit submit，
 * 由父组件 ParamLabView 调 submitReplayAsync。
 *
 * 设计：cfg_override 本地 ref 初始化 = prefill ?? schema defaults；只把「改过/非默认」
 * 的字段进提交体（与后端 _merge_cfg 增量合并语义一致——默认值不传，后端用 StrategyConfig 默认）。
 */
import { computed, ref, watch } from 'vue'
import { PARAM_GROUPS, PARAM_META } from './paramMeta'
import type { ReplayAsyncRequestBody } from '@/api/caisen'

const props = defineProps<{
  visible: boolean
  configSchema: Record<string, any>
  prefill?: Record<string, unknown> | null
  submitting?: boolean
}>()
const emit = defineEmits<{ 'update:visible': [boolean]; submit: [ReplayAsyncRequestBody] }>()

const start = ref('')
const end = ref('')
const universeText = ref('')              // 文本框（逗号/空白分隔），提交时拆为数组
const cfg = ref<Record<string, any>>({})  // 本地可编辑参数

// schema 默认值表（number/integer/boolean）
const defaults = computed<Record<string, any>>(() => {
  const out: Record<string, any> = {}
  for (const [name, f] of Object.entries(props.configSchema?.properties || {})) {
    out[name] = (f as any).default
  }
  return out
})

// 抽屉打开 / prefill 变化时重灌：prefill 优先，否则 schema 默认
watch(() => [props.visible, props.prefill], () => {
  if (!props.visible) return
  cfg.value = { ...defaults.value, ...(props.prefill || {}) }
}, { immediate: true })

// 按 PARAM_GROUPS 分组的字段（仅含 schema 里实际存在的字段）
const groupedFields = computed(() =>
  PARAM_GROUPS.map((g) => ({
    group: g,
    fields: Object.entries(PARAM_META)
      .filter(([name, m]) => m.group === g && props.configSchema?.properties?.[name])
      .map(([name, m]) => ({ name, ...m, spec: props.configSchema.properties[name] })),
  })).filter((g) => g.fields.length),
)

function onSubmit() {
  // 仅传非默认字段（增量覆盖语义）；start/end 必填，universe 留空=全市场(null)
  const cfgOverride: Record<string, any> = {}
  for (const [name, val] of Object.entries(cfg.value)) {
    if (val !== defaults.value[name]) cfgOverride[name] = val
  }
  const universe = universeText.value.trim()
    ? universeText.value.split(/[\s,，]+/).filter(Boolean)
    : null
  emit('submit', { start: start.value, end: end.value, universe, cfg_override: cfgOverride })
}
</script>

<template>
  <el-drawer :model-value="visible" title="新建回测" size="480px"
             @update:model-value="emit('update:visible', $event)">
    <!-- 区间 / 标的池 -->
    <div class="drawer-section">
      <div class="qt-section-title">回测区间与标的</div>
      <el-date-picker v-model="start" type="date" value-format="YYYY-MM-DD" placeholder="开始日" data-testid="start" />
      <el-date-picker v-model="end"   type="date" value-format="YYYY-MM-DD" placeholder="结束日" data-testid="end" />
      <el-input v-model="universeText" type="textarea" :rows="2"
                placeholder="标的池（逗号/空白分隔）；留空=全市场（慢）" />
    </div>

    <!-- 分组参数表单（configSchema 反射 + paramMeta 中文标题） -->
    <el-collapse v-for="g in groupedFields" :key="g.group" v-model="activeGroups" class="drawer-section">
      <el-collapse-item :title="g.group" :name="g.group">
        <div v-for="f in g.fields" :key="f.name" class="param-row">
          <span class="param-label" :title="f.spec?.description">{{ f.title }}</span>
          <!-- bool → switch；number/integer → input-number（带 ge/le 约束）；其余 → input -->
          <el-switch v-if="f.spec?.type === 'boolean'" v-model="cfg[f.name]" />
          <el-input-number v-else-if="['number','integer'].includes(f.spec?.type)"
                           v-model="cfg[f.name]" :min="f.spec?.exclusiveMinimum ?? f.spec?.minimum"
                           :max="f.spec?.maximum" :step="f.spec?.type === 'integer' ? 1 : 0.01"
                           size="small" />
          <el-input v-else v-model="cfg[f.name]" size="small" />
          <span class="param-default">默认 {{ f.spec?.default }}</span>
        </div>
      </el-collapse-item>
    </el-collapse>

    <template #footer>
      <el-button data-testid="submit-replay" type="primary" :loading="submitting"
                 :disabled="!start || !end" @click="onSubmit">提交异步回测</el-button>
    </template>
  </el-drawer>
</template>

<script lang="ts">
// 非响应式默认值：分组折叠默认全展开（import 复用 PARAM_GROUPS）
import { ref } from 'vue'
const activeGroups = ref<string[]>([])
export default {}   // 仅占位，实际 setup 在上方
</script>

<style scoped>
.drawer-section { margin-bottom: var(--qt-space-3); }
.param-row { display: flex; align-items: center; gap: var(--qt-space-2); margin: 4px 0; }
.param-label { width: 150px; color: var(--qt-text-regular); font-size: 12px; }
.param-default { color: var(--qt-text-secondary); font-size: 11px; }
</style>
```

> 实现要点：上方双 `<script>` 块的 `activeGroups` 放进 `<script setup>` 顶部（`const activeGroups = ref(PARAM_GROUPS)` 默认全展开），删除占位 `<script lang="ts">` 块——示例为避免与 setup 冲突拆写，实际合并到 setup。`el-collapse v-model` 用 ref 数组控制展开组。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd web && npm run test -- NewReplayDrawer && npm run typecheck`
Expected: PASS（3 用例绿；typecheck 无错）

- [ ] **Step 5: 提交**

```bash
git add web/src/components/lab/NewReplayDrawer.vue web/src/components/lab/NewReplayDrawer.spec.ts
git commit -m "feat(lab): NewReplayDrawer 新建回测抽屉（分组参数表单，Spec 2 Task 5）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 6: ParamLabView.vue + 路由 + 导航（主画布）

**Files:**
- Create: `web/src/views/ParamLabView.vue`
- Modify: `web/src/router/index.ts`（加 /lab 路由）
- Modify: `web/src/App.vue`（researchNav 加「参数实验室」）
- Test: `web/src/views/ParamLabView.spec.ts`（mount 渲染冒烟）

**Interfaces:**
- Consumes: T2（5 个异步 API 函数 + 类型）、T3（paramMeta）、T4（ReplayReportPanel）、T5（NewReplayDrawer）、`getConfigSchema`
- Produces: `/lab` 路由可访问、4 区画布 + master-detail + 轮询 + 抽屉编排

- [ ] **Step 1: 写失败测试（mount 冒烟）**

Create `web/src/views/ParamLabView.spec.ts`（mock apiClient，避免真发请求）：

```ts
import { describe, it, expect, vi } from 'vitest'
import { mount, flushPromises } from '@vue/test-utils'
import ElementPlus from 'element-plus'
import ParamLabView from './ParamLabView.vue'

vi.mock('@/api/caisen', () => ({
  getConfigSchema: vi.fn().mockResolvedValue({ properties: { min_rr_ratio: { type: 'number', default: 1.5 } } }),
  listReplayTasks: vi.fn().mockResolvedValue([]),
  getReplayTask: vi.fn().mockResolvedValue(null),
}))

class MockObserver { observe() {} unobserve() {} disconnect() {} takeRecords() { return [] } }
;(globalThis as any).ResizeObserver = MockObserver
;(globalThis as any).IntersectionObserver = MockObserver
;(globalThis as any).matchMedia = (globalThis as any).matchMedia || ((q: string) => ({
  matches: false, media: q, onchange: null, addListener: vi.fn(), removeListener: vi.fn(),
  addEventListener: vi.fn(), removeEventListener: vi.fn(), dispatchEvent: vi.fn(),
}))

describe('ParamLabView', () => {
  it('渲染 4 区 + 新建回测按钮 + 空态提示（无任务时）', async () => {
    const wrapper = mount(ParamLabView, { global: { plugins: [ElementPlus] } })
    await flushPromises()
    expect(wrapper.text()).toContain('参数详情')
    expect(wrapper.text()).toContain('收益率走势')
    expect(wrapper.text()).toContain('买卖日志')
    expect(wrapper.text()).toContain('任务列表')
    expect(wrapper.text()).toContain('新建回测')
    // 空态
    expect(wrapper.text()).toContain('点 ＋新建回测')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd web && npm run test -- ParamLabView`
Expected: FAIL（视图不存在）

- [ ] **Step 3: 加路由 + 导航**

`web/src/router/index.ts`：
```ts
const ParamLabView = () => import('../views/ParamLabView.vue')
// routes 数组（/caisen 后插入）：
{ path: '/lab', name: 'lab', component: ParamLabView },
```

`web/src/App.vue` import 加 `DataAnalysis` 图标；`researchNav`「蔡森筛选」后插：
```ts
{ to: '/lab', label: '参数实验室', icon: DataAnalysis },
```

- [ ] **Step 4: 实现 ParamLabView.vue**

```vue
<script setup lang="ts">
/**
 * 参数实验室主视图（/lab）。Spec 2 核心。
 *
 * 画布（纵向堆叠）：顶栏（选中标识/状态筛选/＋新建回测）→ 左参数详情(只读)｜右收益走势+统计
 * → 买卖日志 → 任务列表(master)。点任务行 master-detail 灌入上方三区。
 *
 * 轮询：存在 PENDING/RUNNING 时每 3s 刷 list；选中任务状态/进度变化时重取详情。无活跃任务停轮询。
 * 边界（spec §11）：404 清选中、422 留抽屉、FAILED 显 error、取消竞态轮询到 CANCELLED、空态提示。
 */
import { computed, onMounted, onUnmounted, ref, shallowRef } from 'vue'
import { ElMessage, ElMessageBox } from 'element-plus'
import {
  getConfigSchema, listReplayTasks, getReplayTask,
  submitReplayAsync, cancelReplayTask, deleteReplayTask,
} from '@/api/caisen'
import type { ReplayTask, ReplayTaskDetail, ReplayTaskStatus, ReplayAsyncRequestBody } from '@/api/caisen'
import { PARAM_GROUPS, PARAM_META } from '@/components/lab/paramMeta'
import ReplayReportPanel from '@/components/lab/ReplayReportPanel.vue'
import NewReplayDrawer from '@/components/lab/NewReplayDrawer.vue'
import { logger } from '@/utils/logger'

const POLL_MS = 3000
const configSchema = shallowRef<Record<string, any>>({})
const tasks = ref<ReplayTask[]>([])
const selectedId = ref<string | null>(null)
const selected = shallowRef<ReplayTaskDetail | null>(null)
const statusFilter = ref<ReplayTaskStatus | ''>('')
const drawerVisible = ref(false)
const submitting = ref(false)
let pollTimer: ReturnType<typeof setInterval> | null = null

// —— 参数详情（只读）：schema 默认 ∪ 选中任务 cfg_override，按 PARAM_GROUPS 分组 ——
const groupedParamValues = computed(() =>
  PARAM_GROUPS.map((g) => ({
    group: g,
    fields: Object.entries(PARAM_META)
      .filter(([name, m]) => m.group === g && configSchema.value.properties?.[name])
      .map(([name, m]) => {
        const overridden = selected.value?.cfg_override?.[name]
        const def = configSchema.value.properties[name].default
        return { name, title: m.title, value: overridden ?? def, isDefault: overridden === undefined, desc: configSchema.value.properties[name].description }
      }),
  })).filter((g) => g.fields.length),
)

const hasActive = computed(() => tasks.value.some((t) => t.status === 'PENDING' || t.status === 'RUNNING'))
const filteredTasks = computed(() =>
  statusFilter.value ? tasks.value.filter((t) => t.status === statusFilter.value) : tasks.value,
)

async function loadSchema() {
  try { configSchema.value = await getConfigSchema() }
  catch (e) { logger.error('加载 config schema 失败:', e) }
}
async function loadTasks() {
  try {
    tasks.value = await listReplayTasks()
    // 默认选中最新一条（首次或选中被删时）
    if (!selectedId.value && tasks.value.length) await selectTask(tasks.value[0].task_id)
  } catch (e) { logger.error('加载任务列表失败:', e) }
}
async function selectTask(taskId: string) {
  selectedId.value = taskId
  try {
    selected.value = await getReplayTask(taskId)
  } catch (e: any) {
    // 404（被删/不存在）：清选中 + 提示 + 刷新
    selected.value = null
    selectedId.value = null
    ElMessage.warning('该任务已不存在')
    await loadTasks()
  }
}

async function refreshSelectedIfChanged() {
  if (!selectedId.value) return
  const row = tasks.value.find((t) => t.task_id === selectedId.value)
  if (!row) return
  const cur = selected.value
  // 选中任务状态变化或进度推进 → 重取详情（拿 SUCCESS 的 report）
  if (!cur || row.status !== cur.status || row.progress !== cur.progress) {
    try { selected.value = await getReplayTask(selectedId.value) }
    catch (e) { logger.error('刷新选中详情失败:', e) }
  }
}

async function poll() {
  await loadTasks()
  await refreshSelectedIfChanged()
  // 无活跃任务 → 停轮询（省请求）
  if (!hasActive.value && pollTimer) { clearInterval(pollTimer); pollTimer = null }
}
function ensurePolling() {
  if (hasActive.value && !pollTimer) pollTimer = setInterval(poll, POLL_MS)
}

// —— 新建回测（抽屉提交） ——
async function onSubmit(body: ReplayAsyncRequestBody) {
  submitting.value = true
  try {
    const { task_id } = await submitReplayAsync(body)
    ElMessage.success('已提交，task_id=' + task_id)
    drawerVisible.value = false
    await loadTasks()
    await selectTask(task_id)   // 选中新任务并起轮询
    ensurePolling()
  } catch (e: any) {
    // 422 参数非法：留抽屉，展示后端错误
    ElMessage.error('提交失败：' + (e?.response?.data?.detail || e.message || e))
  } finally {
    submitting.value = false
  }
}

async function onCancel(t: ReplayTask) {
  try { await cancelReplayTask(t.task_id); ElMessage.info('取消信号已发送') }
  catch (e: any) { ElMessage.error('取消失败：' + (e?.response?.data?.detail || e)) }
}
async function onDelete(t: ReplayTask) {
  await ElMessageBox.confirm('确认删除该任务记录？', '删除确认', { type: 'warning' })
  try {
    await deleteReplayTask(t.task_id)
    if (selectedId.value === t.task_id) { selectedId.value = null; selected.value = null }
    await loadTasks()
    ElMessage.success('已删除')
  } catch (e: any) { ElMessage.error('删除失败：' + (e?.response?.data?.detail || e)) }
}

onMounted(async () => {
  await Promise.all([loadSchema(), loadTasks()])
  ensurePolling()
})
onUnmounted(() => { if (pollTimer) clearInterval(pollTimer) })
</script>

<template>
  <div class="qt-view-shell lab-view">
    <!-- 顶栏 -->
    <div class="lab-topbar">
      <span class="lab-title">参数实验室
        <span v-if="selected" class="lab-sub">· {{ selected.status }} · {{ selected.start }}~{{ selected.end }} · {{ selected.universe_n === -1 ? '全市场' : (selected.universe_n + ' 只') }}</span>
      </span>
      <el-select v-model="statusFilter" placeholder="全部状态" size="small" clearable style="width:120px">
        <el-option label="运行中" value="RUNNING" />
        <el-option label="已完成" value="SUCCESS" />
        <el-option label="失败" value="FAILED" />
        <el-option label="已取消" value="CANCELLED" />
        <el-option label="待运行" value="PENDING" />
      </el-select>
      <el-button type="primary" size="small" @click="drawerVisible = true">＋ 新建回测</el-button>
    </div>

    <!-- 第1层：左参数详情(只读) ｜ 右收益走势+统计 -->
    <div class="lab-row lab-row-top">
      <div class="qt-card lab-params">
        <div class="qt-section-title">参数详情</div>
        <div v-for="g in groupedParamValues" :key="g.group" class="param-group">
          <div class="param-group-name">{{ g.group }}</div>
          <div v-for="f in g.fields" :key="f.name" class="param-line" :title="f.desc">
            <span class="param-line-title">{{ f.title }}</span>
            <span class="param-line-val" :class="{ overridden: !f.isDefault }">{{ f.value }}</span>
          </div>
        </div>
      </div>
      <div class="qt-card lab-chart">
        <div class="qt-section-title">收益率走势</div>
        <!-- SUCCESS：画曲线 + 统计卡（复用 ReplayReportPanel） -->
        <ReplayReportPanel v-if="selected?.report" :report="selected.report" />
        <!-- RUNNING/PENDING：进度占位 -->
        <div v-else-if="selected && (selected.status === 'RUNNING' || selected.status === 'PENDING')"
             class="qt-empty">回测中 {{ selected.progress }}%…</div>
        <!-- FAILED：显 error -->
        <div v-else-if="selected?.status === 'FAILED'" class="qt-empty lab-failed">
          回测失败：{{ selected.error }}
          <el-button size="small" @click="drawerVisible = true">以此参数重提</el-button>
        </div>
        <div v-else class="qt-empty">选择一个任务查看结果</div>
      </div>
    </div>

    <!-- 第2层：买卖日志（ReplayReportPanel 已含流水表；此处独立区便于布局/空态） -->
    <div class="qt-card lab-trades">
      <div class="qt-section-title">买卖日志</div>
      <div v-if="selected?.report?.trades?.length" class="lab-trades-list">
        <!-- 流水行：标的/形态/买卖日价/离场原因/盈亏比/持仓天 -->
        <div v-for="(t, i) in selected.report.trades" :key="i" class="trade-row">
          <span>{{ t.symbol }}</span><span>{{ t.pattern_type }}</span>
          <span>{{ t.entry_date }}/{{ t.entry_price }}</span>
          <span>{{ t.exit_date }}/{{ t.exit_price }}</span>
          <span>{{ t.exit_reason }}</span>
          <span :class="t.rr >= 0 ? 'up' : 'down'">{{ t.rr.toFixed(2) }}</span>
          <span>{{ t.holding_bars }}天</span>
        </div>
      </div>
      <div v-else class="qt-empty">暂无买卖日志</div>
    </div>

    <!-- 第3层：任务列表（master） -->
    <div class="qt-card lab-tasks">
      <div class="qt-section-title">任务列表</div>
      <div v-if="filteredTasks.length" class="lab-task-list">
        <div v-for="t in filteredTasks" :key="t.task_id" class="task-row"
             :class="{ active: t.task_id === selectedId }" @click="selectTask(t.task_id)">
          <span class="task-status" :class="'st-' + t.status">{{ t.status }}</span>
          <span>{{ t.start }}~{{ t.end }}</span>
          <span>{{ t.universe_n === -1 ? '全市场' : (t.universe_n + '只') }}</span>
          <span>{{ t.cfg_override?.min_rr_ratio ?? '默认' }}</span>
          <span>{{ t.progress }}%</span>
          <span class="task-ai">[AI Spec3]</span>
          <span class="task-actions">
            <el-button v-if="t.status === 'RUNNING' || t.status === 'PENDING'" size="small" @click.stop="onCancel(t)">取消</el-button>
            <el-button v-if="t.status !== 'RUNNING'" size="small" type="danger" @click.stop="onDelete(t)">删除</el-button>
          </span>
        </div>
      </div>
      <div v-else class="qt-empty">点 ＋新建回测 开始第一次实验</div>
    </div>

    <!-- 新建回测抽屉 -->
    <NewReplayDrawer v-model:visible="drawerVisible" :config-schema="configSchema"
                     :prefill="selected?.cfg_override" :submitting="submitting" @submit="onSubmit" />
  </div>
</template>

<style scoped>
.lab-view { display: flex; flex-direction: column; gap: var(--qt-space-2); padding: var(--qt-space-3); height: 100%; overflow: auto; }
.lab-topbar { display: flex; align-items: center; gap: var(--qt-space-2); }
.lab-title { font-weight: 600; color: var(--qt-text-primary); }
.lab-sub { color: var(--qt-text-secondary); font-weight: 400; font-size: 12px; }
.lab-row-top { display: grid; grid-template-columns: 360px 1fr; gap: var(--qt-space-2); }
.lab-params, .lab-chart, .lab-trades, .lab-tasks { padding: var(--qt-space-3); }
.param-group-name { color: var(--qt-accent); font-size: 12px; margin: var(--qt-space-2) 0 2px; }
.param-line { display: flex; justify-content: space-between; font-size: 12px; color: var(--qt-text-regular); padding: 1px 0; }
.param-line-val.overridden { color: var(--qt-up); font-weight: 600; }   /* 改过默认的值高亮（非 A 涨跌语义，仅强调） */
.lab-trades-list, .lab-task-list { display: flex; flex-direction: column; font-size: 12px; }
.trade-row, .task-row { display: grid; grid-template-columns: repeat(7, 1fr); gap: var(--qt-space-2); padding: 3px 0; border-bottom: 1px solid var(--qt-border); }
.task-row { cursor: pointer; grid-template-columns: 70px 1fr 70px 70px 60px 70px 120px; align-items: center; }
.task-row.active { background: rgba(41,98,255,0.12); }
.task-row:hover { background: var(--qt-bg-elevated); }
.up { color: var(--qt-up); } .down { color: var(--qt-down); }
.st-SUCCESS { color: var(--qt-up); } .st-FAILED, .st-CANCELLED { color: var(--qt-down); }
.st-RUNNING { color: var(--qt-accent); } .st-PENDING { color: var(--qt-text-secondary); }
.lab-failed { color: var(--qt-down); }
</style>
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd web && npm run test -- ParamLabView && npm run typecheck`
Expected: PASS（冒烟用例绿；typecheck 无错）

- [ ] **Step 6: 提交**

```bash
git add web/src/views/ParamLabView.vue web/src/views/ParamLabView.spec.ts web/src/router/index.ts web/src/App.vue
git commit -m "feat(lab): ParamLabView 主画布 + /lab 路由 + 导航（Spec 2 Task 6）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 7: /lab 端到端 E2E

**Files:**
- Create: `tests/e2e/lab_param_lab.py`

**Interfaces:**
- Consumes: with_server.py（webapp-testing skill）+ `.venv310` python
- Produces: 真实浏览器验证 /lab 提交→轮询→SUCCESS→结果渲染链路

- [ ] **Step 1: 写 E2E 脚本**

Create `tests/e2e/lab_param_lab.py`（仿 `caisen_replay_tab.py` 结构；小 universe 快速回测）：

```python
# -*- coding: utf-8 -*-
"""/lab 参数实验室 E2E：提交异步回测→轮询→SUCCESS→结果渲染（记忆 default-e2e-after-ui）。

验证 Spec 2 端到端：goto /lab → 开「新建回测」抽屉 → 填小 universe + 改一个参数 →
提交 → 轮询等 SUCCESS → 断言参数详情/走势/买卖日志渲染 + 无 pageerror + 截图。

运行（with_server 起后端 + 前端）：
  VENV=.venv310/Scripts/python.exe
  QUANTER_API_TOKEN=e2e-token python "$WITH_SERVER" \\
    --server "$VENV -m uvicorn server.main:app --port 8000" --port 8000 \\
    --server "npm --prefix web run dev" --port 5173 --timeout 180 \\
    -- "$VENV" tests/e2e/lab_param_lab.py
"""
import sys

URL = "http://localhost:5173/lab"
# 前 5 只 A 股 + 短区间（异步回测快；命中数不论，验 UI 链路）
UNIVERSE = ",".join(["000001.SZ", "000002.SZ", "000063.SZ", "000066.SZ", "000100.SZ"])


def main() -> int:
    from playwright.sync_api import sync_playwright

    page_errors, console_errors = [], []

    with sync_playwright() as p:
        browser = p.chromium.launch(headless=True)
        page = browser.new_page()
        page.on("pageerror", lambda e: page_errors.append(str(e)))
        page.on("console", lambda m: console_errors.append(m.text) if m.type == "error" else None)

        # 1) 加载 /lab
        page.goto(URL, wait_until="domcontentloaded")
        page.wait_for_load_state("networkidle")

        # 2) 断言 4 区 + 新建按钮渲染
        for txt in ("参数详情", "收益率走势", "买卖日志", "任务列表", "新建回测"):
            assert page.locator(f"text={txt}").count() > 0, f"缺区域：{txt}"

        # 3) 开抽屉 + 填区间/universe + 改 min_rr_ratio=1.0
        page.get_by_role("button", name="新建回测").click()
        page.wait_for_selector(".el-drawer", timeout=5000)
        page.locator("[data-testid=start] input, [data-testid=start]").first.fill("2024-01-01")
        page.locator("[data-testid=end] input, [data-testid=end]").first.fill("2024-06-01")
        page.locator(".el-drawer textarea").first.fill(UNIVERSE)
        # 提交
        page.get_by_role("button", name="提交异步回测").click()

        # 4) 轮询等 SUCCESS（异步回测 + 装配，给足 180s）
        page.wait_for_selector("text=已完成", timeout=180000)
        page.wait_for_timeout(1500)   # 等结果渲染稳定

        # 5) 截图 + 断言走势/统计已渲染（ReplayReportPanel 资金曲线）
        page.screenshot(path="tests/e2e/_lab_param_lab.png", full_page=True)
        # 任务列表至少 1 行 SUCCESS
        assert page.locator(".st-SUCCESS").count() >= 1, "无 SUCCESS 任务"

        browser.close()

    print(f"[结果] pageerror={len(page_errors)}, console error={len(console_errors)}")
    for e in page_errors[:5]:
        print(f"  [pageerror] {e}")
    ok = len(page_errors) == 0
    print("[PASS] /lab 提交→轮询→SUCCESS→结果渲染链路通" if ok else "[FAIL] 链路异常")
    return 0 if ok else 1


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: 跑 E2E（with_server 起前后端）**

Run:
```bash
QUANTER_API_TOKEN=e2e-token .venv310/Scripts/python.exe "$WITH_SERVER" \
  --server ".venv310/Scripts/python.exe -m uvicorn server.main:app --port 8000" --port 8000 \
  --server "npm --prefix web run dev" --port 5173 --timeout 180 \
  -- ".venv310/Scripts/python.exe" tests/e2e/lab_param_lab.py
```
（`$WITH_SERVER` = webapp-testing skill 的 `scripts/with_server.py` 绝对路径；Windows 尾串 `&& python scripts/clean_ports.py` 清残留）
Expected: `[PASS] /lab 提交→轮询→SUCCESS→结果渲染链路通` + 截图 `tests/e2e/_lab_param_lab.png` 生成。

- [ ] **Step 3: 提交**

```bash
git add tests/e2e/lab_param_lab.py
git commit -m "test(e2e): /lab 参数实验室端到端实测（Spec 2 Task 7）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 8: 下线 /caisen 老「历史回放」tab + 清理死代码

**Files:**
- Modify: `web/src/views/CaisenScreenView.vue`（删回放 tab + state + methods，审核区铺平）
- Modify: `web/src/api/caisen.ts`（删前端不再调的老同步函数 + 类型）
- Modify: `web/src/api/caisen.spec.ts`（删老函数用例）
- Modify: `tests/e2e/caisen_replay_tab.py`（改造为「回放 tab 已移除 + 扫描链路正常」回归守护）

**Interfaces:**
- Consumes: T2-T7（/lab 已接管回放能力，老 tab 可安全删）
- Produces: /caisen 回归纯扫描/审核/激活；caisen.ts 无死代码

- [ ] **Step 1: 改造 caisen_replay_tab.py E2E 为回归守护（先改测试，定义"完成态"）**

`tests/e2e/caisen_replay_tab.py` 的 `main()` 改为：加载 /caisen → 断言**无**「历史回放」tab → 断言扫描链路仍可用（候选计划表/审核区存在）+ 无 pageerror + 截图。替换 `main()` 体：

```python
def main() -> int:
    from playwright.sync_api import sync_playwright
    page_errors, console_errors = [], []
    with sync_playwright() as p:
        browser = p.chromium.launch(headless=True)
        page = browser.new_page()
        page.on("pageerror", lambda e: page_errors.append(str(e)))
        page.on("console", lambda m: console_errors.append(m.text) if m.type == "error" else None)
        page.goto(URL, wait_until="domcontentloaded")
        page.wait_for_load_state("networkidle")

        # 回归守护：历史回放 tab 已移除（Spec 2 下线）
        replay_tab = page.locator(".el-tabs__item").filter(has_text="历史回放")
        tab_removed = replay_tab.count() == 0
        # 扫描/审核链路仍在（候选计划区或审核入口存在，页面可用）
        page.screenshot(path="tests/e2e/_caisen_replay_tab.png", full_page=True)
        browser.close()

    print(f"[结果] pageerror={len(page_errors)}")
    ok = len(page_errors) == 0 and tab_removed
    print(f"[回放tab已移除]={tab_removed}")
    print("[PASS] /caisen 回放 tab 已下线、扫描链路正常" if ok else "[FAIL]")
    return 0 if ok else 1
```
（保留文件顶部的 `URL`/`UNIVERSE` 常量与模块 docstring，仅更新断言语义。）

- [ ] **Step 2: 跑该 E2E 确认当前失败（tab 还在）**

Run: 同 Task 7 的 with_server 命令，脚本换 `tests/e2e/caisen_replay_tab.py`
Expected: `[FAIL]`（tab 仍在——定义了完成态）

- [ ] **Step 3: 从 CaisenScreenView 删除回放 tab**

`web/src/views/CaisenScreenView.vue`：
- 删「历史回放」`el-tab-pane`（含其内 `.replay-area` 全部模板：回放参数表单/历史回测记录折叠/`<ReplayReportPanel>`）。
- 删回放相关 state：`replayForm` / `replaying` / `replayReport` / `replayRuns` / `currentRunId`。
- 删回放相关方法：`onReplay` / `loadReplayRuns` / `loadReplayRun` / `deleteReplayRun`。
- `activeTab`（`'review'|'replay'`）失去意义 → 删 `el-tabs` 结构，审核区（候选计划表 + approve/reject/activate）直接铺平为底部主区；删 `activeTab` ref。
- 删 import 中 `runReplay` / `listReplayRuns` / `getReplayRun` / `deleteReplayRun` / `ReplayRunSummary`（保留 `getConfigSchema`？/caisen 不再用则一并删 import；`ReplayReportPanel` 若仅回放 tab 用则删 import）。
- onMounted 内 `loadReplayRuns()` 调用删除。

> 用 grep 确认无残留引用：`grep -n "replay\|Replay\|activeTab" web/src/views/CaisenScreenView.vue` 应只剩注释（无活引用）。

- [ ] **Step 4: 删 caisen.ts 老同步死函数 + 类型**

`web/src/api/caisen.ts` 删除（前端不再消费）：
- 函数：`runReplay` / `listReplayRuns` / `getReplayRun` / `deleteReplayRun`
- 类型：`ReplayRunSummary` / `ReplayRunDetail` / `ReplayRequestBody`
- 保留：`getConfigSchema`（/lab 用）、`ReplayReport`/`EquityPoint`/`Trade`（任务详情 report 复用）、异步任务 5 函数（T2）。

先 grep 确认无其他 importer：`grep -rn "runReplay\|listReplayRuns\|getReplayRun\|deleteReplayRun\|ReplayRunSummary\|ReplayRunDetail\|ReplayRequestBody" web/src/` 应只在 caisen.ts + caisen.spec.ts（T8 内删）命中。

- [ ] **Step 5: 删 caisen.spec.ts 老函数用例**

`web/src/api/caisen.spec.ts`：删 `runReplay: POST .../replay` 用例 + import 中的 `runReplay`。

- [ ] **Step 6: 校验——typecheck + vitest + 两 E2E + 后端全绿**

Run:
```bash
cd web && npm run typecheck && npm run test
.venv310/Scripts/python.exe -m pytest -q   # 后端 700 绿（本次仅加 DELETE，应不回归）
# /caisen 退役 E2E：
QUANTER_API_TOKEN=e2e-token .venv310/Scripts/python.exe "$WITH_SERVER" \
  --server ".venv310/Scripts/python.exe -m uvicorn server.main:app --port 8000" --port 8000 \
  --server "npm --prefix web run dev" --port 5173 --timeout 120 \
  -- ".venv310/Scripts/python.exe" tests/e2e/caisen_replay_tab.py
```
Expected: typecheck 无错；vitest 全绿；pytest 全绿；caisen_replay_tab.py 输出 `[PASS] /caisen 回放 tab 已下线`。

- [ ] **Step 7: 提交**

```bash
git add web/src/views/CaisenScreenView.vue web/src/api/caisen.ts web/src/api/caisen.spec.ts tests/e2e/caisen_replay_tab.py
git commit -m "refactor(lab): 下线 /caisen 老「历史回放」tab + 清理死代码（Spec 2 Task 8）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec 覆盖**
- /lab 独立路由 + 画布 + 抽屉 + master-detail + 轮询 → T6 ✓
- 后端 DELETE /replay/tasks/{id} → T1 ✓
- 异步任务 client 5 函数 → T2 ✓
- 中文参数映射 + 同步守护 → T3 ✓
- ReplayReportPanel 抽取复用 → T4 ✓
- 新建回测抽屉（configSchema 反射 + 分组 + prefill）→ T5 ✓
- 路由 + 导航 → T6 ✓
- 下线 /caisen 老 tab + 死代码清理 → T8 ✓
- E2E（/lab + /caisen 退役）→ T7, T8 ✓
- 边界（404/422/FAILED/取消竞态/空态）→ T6 实现 ✓
- design token 约束 → 各前端任务样式段 ✓
- paramMeta 同步守护 → T3 ✓

**2. 占位符扫描**：无 TBD/TODO；NewReplayDrawer 示例的双 `<script>` 块已在「实现要点」注明合并到 setup（非占位，是明确指令）。equityOption 的 series/title 为示例最小值，实现时从 CaisenScreenView 逐字迁入（T4 已说明）。

**3. 类型一致性**：`ReplayTask`/`ReplayTaskDetail`/`ReplayAsyncRequestBody`/`CancelResponse`（T2 定义）在 T5/T6 消费处名称一致；`PARAM_META`/`PARAM_GROUPS`（T3）在 T5/T6 消费处一致；`submitReplayAsync`/`listReplayTasks`/`getReplayTask`/`cancelReplayTask`/`deleteReplayTask`（T2）在 T6 调用处一致；`ReplayReportPanel` props.report（T4）在 T6 消费处一致。

---

## Execution Handoff

计划已保存至 `docs/superpowers/plans/2026-07-14-param-lab.md`。两种执行方式：

**1. Subagent-Driven（推荐）** —— 每任务派一个新 subagent，任务间 review，迭代快、上下文干净。

**2. Inline Execution** —— 本会话内用 executing-plans 批量执行 + 检查点 review。

选哪种？
