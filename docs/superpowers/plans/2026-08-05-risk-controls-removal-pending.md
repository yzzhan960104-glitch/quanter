# 决策记录：风控阈值移除（待 spec 整体整理）

- 日期：2026-08-05
- 来源：研究员在 Codex 对话中的 Annotation 1 指令
- 状态：**仅记录，未实施**（不改代码、不改 .env）
- 后续：已并入 `docs/superpowers/specs/2026-08-05-process-gateway-ssot-final-design.md`
  （Phase A · 2026-08-05）；实施前置与边界见该 spec §2.3 / §7.1

## 决策内容

0. **废弃删除当前风控所有逻辑**（不仅限于下述阈值，含 10 关下单挡板、网关/熔断层、
   引擎流程闸、计划确认/veto、启动影子闸、白名单、订单状态机等全部拦截逻辑），
   待后续链路调通后整体重构。

以下风控拦截项全部需要去掉（当前仅记录，后面整体整理 spec 再实施）：

1. `QMT_ENFORCE_SESSION=true` —— A 股交易时段拦截（09:30–11:30 / 13:00–15:00）
2. `QMT_ORDER_MAX_AMOUNT=200000` —— 单笔金额上限
3. `QMT_ORDER_MAX_SHARES=10000` —— 单笔股数上限
4. `QMT_SYMBOL_WHITELIST` 静态白名单（510300.SH / 511010.SH / 510500.SH / 159915.SZ）及对应白名单拦截；
   动态白名单注入/清空机制是否一并移除，由后续 spec 确定
5. `TRADE_SHADOW_MIN_DAYS=1` —— 影子期硬闸（`check_shadow_gate`）；整个闸门是否移除或仅改阈值，由后续 spec 确定
6. `CIRCUIT_DAILY_LOSS_LIMIT=-0.03` —— 日内权益回撤 -3% 熔断判定

## 背景

- 2026-08-05 09:22 pre_open 挂单 300358.SZ 被 session 关拦截（`非 A 股交易时段`），
  订单未送达 QMT 柜台。
- 研究员据此要求移除上述风控阈值；本文件只做决策留痕，不落地任何改动。

## 风险提示（供 spec 整理时评估）

- 上述多为安全兜底（防隔夜/周末误单、单笔敞口、未成熟实验进 LIVE、日内大幅回撤）。
- 移除前需在 spec 中明确：以什么机制替代，或明确接受裸奔边界。
- session 关的替代方案至少要考虑：pre_open 09:22 集合竞价窗口应允许挂单（修窗口起点
  09:30 → 09:15 是另一条路径，与“整体移除”二选一，由 spec 定）。

## 记录范围

- 本文件仅记录意图，不修改 `trading/`、`broker/`、`.env` 任何内容。
