# W1-A T2 keystone 实施计划（切断 _eng_mod 反查 + 下沉 trading_service + 删 re-export + 迁 360 patch）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 切断 trading 内 `_eng_mod` 反查（phases/order_state 不再 `import trading.engine as _eng_mod`）+ 下沉 `trading_service.py` → `trading/gateway_service.py` + 删 engine re-export 块 + 迁移 360 处测试 patch 到物理路径——一次手术治 spec §0.3 统一根因（trading concern 散落错误模块 + lazy import 规避循环）。

**Architecture:** 4 phases。Phase 0 环验证（order_state→phases 是否成环）+ blackout 收口形态。Phase 1 物理迁移（trading_service 下沉 + _submit/get_gateway 随之 + blackout throttle）。Phase 2 切断 _eng_mod 反查（phases/order_state 改直接 import 物理模块，按 19 符号差异化策略）。Phase 3 删 engine re-export 块 + 360 patch 按文件分批迁移。Phase 4 验证（_eng_mod grep 0 + e2e + 全量）。

**Tech Stack:** Python 3.10（`.venv310`）、pytest、纯 stdlib（dataclass/Protocol）。无新依赖。

## Global Constraints

- 全中文注释，像素级说明 Why（CLAUDE.md）。
- Karpathy 极简：纯标准库 + dataclass/Protocol，**不引入** DI 容器/事件总线/ORM（spec §0.2 硬约束 2）。
- **行为等价红线（spec §0.2 硬约束 5）**：_eng_mod 切断后行为逐字一致；patch 迁移只改路径不改断言；状态机/数据路径不变形。**每个切断 Task 必过 L4 双跑**（重构前后同输入 diff，spec §9）。
- **循环 import 纪律**：每个切断 Task 后跑 `python -c "import trading.phases.X"` 逐模块验证无环。
- 渐进式（spec §0.2 硬约束 4）：每 Task 独立 commit + 小步可 revert。
- **file:line 基准 = 2026-08-12**（调研报告 re-verified）。实施时 re-verify 行号，以符号名定位为准。
- 测试：`.venv310/Scripts/python.exe -m pytest`（Windows，设 `PYTHONIOENCODING=utf-8 PYTHONUTF8=1` 避免 GBK artifact）。合并门 = L1 全量绿 + L3 e2e_long_cycle + L4 双跑 + `_eng_mod` grep 0。

## File Structure

| 文件 | 职责 | 本计划改动 |
|---|---|---|
| `presentation/server/services/trading_service.py` | 网关单例 + HTTP 业务（presentation L5） | **整体下沉** → `trading/gateway_service.py`；删孤儿 `PROJECT_ROOT` import；原位留删除标记 |
| `trading/gateway_service.py`（新建） | 网关单例 + 下单/持仓/归因（trading 内部 concern） | **新建**（trading_service.py 1:1 搬迁） |
| `trading/engine.py` | 交易引擎编排 | 删 re-export 块（L78-262）+ 删 `_eng_mod` 相关注释 + blackout throttle 构造 + 8 处 lazy import 改指 gateway_service |
| `trading/ports.py` | EnginePorts dataclass | 加 `blackout: QuoteBlackoutThrottle` 字段 |
| `trading/alerting.py`（新建） | QuoteBlackoutThrottle dataclass | **新建**（blackout 节流收口） |
| `trading/phases/{pre_open,stop_loss,post_close,exit}.py` | phases 子模块 | 删 `_eng_mod` lazy import + 顶部直接 import 物理模块 + stop_loss 经 ports.blackout |
| `trading/order_state.py` | 订单回调/状态机 | 删 `_eng_mod` lazy import + 顶部直接 import |
| `trading/account.py` | account_id SSoT | 无改（已收口） |
| `tests/trading/test_*.py`（top 10 + 余） | 测试 | **360 处 patch 迁移**到物理路径（按映射表） |
| `presentation/server/{api/v1/ops.py,api/v1/trading.py,main.py,services/review_service.py}` | 调用方 | 4 处改指 `trading.gateway_service` |

---

## 符号 → 切断策略表（19 符号，调研 §2 派生）

| 符号 | 策略 | 物理路径（patch 迁移目标） |
|---|---|---|
| `_state_store` | 直接 import 叶子 | `trading.state_store` |
| `_mode` / `_alert_critical` / `_CriticalHalt` / `_critical_guard` / `_trade_cfg` | 直接 import 叶子 | `trading.critical.*` |
| `_resolve_account_id` | 直接 import SSoT | `trading.account.resolve_account_id` |
| `qmt_market_data` | 直接 import 叶子 | `trading.qmt_market_data` |
| `_trading_days_between` | 直接 import 叶子 | `trading.compute.stop.trading_days_between` |
| `_cancel_all_open_orders` | 直接 import 叶子 | `trading.io.breaker.cancel_all_open_orders` |
| `calendar` | 直接 import 叶子 | `trading.calendar` |
| `decide_exit` / `ExitAction` / `ExitReason` | 直接 import 叶子 | `strategies.neckline.execution.*` |
| `_submit` / `get_gateway` | 随 trading_service 下沉 | `trading.gateway_service.{submit_order,get_gateway}`（下沉后） |
| `place_take_profit` | phases 内部互调 | `trading.phases.exit.place_take_profit` |
| `_pre_open_impl` | 同模块直调 | pre_open.py 内删 `_eng_mod.` 前缀 |
| `scan_expired_positions` / `close_expired_positions` | phases 内部互调 | `trading.phases.stop_loss.*` |
| `seq_for_real_oid` / `order_state_to_db` | phases 内部互调（**Task 1 验证无环**） | `trading.phases.post_close.*`（或下沉 order_id_seq.py 若成环） |
| `_last_quote_blackout_alert_ts` / `_QUOTE_BLACKOUT_ALERT_INTERVAL_S` | 收口 QuoteBlackoutThrottle + Ports | `trading.alerting.QuoteBlackoutThrottle` |

## patch 路径映射表（engine.X → 物理.Y，迁 360 patch 用）

| engine patch 路径 | 物理路径 |
|---|---|
| `trading.engine.load_plan` | `trading.trading_plan.load_plan` |
| `trading.engine.trading_plan` / `.trading_plan.load_plan` | `trading.trading_plan` / `.load_plan` |
| `trading.engine.get_data_ready` / `get_ready` | `trading.state_store.get_data_ready` / `get_ready` |
| `trading.engine._cancel_all_open_orders` | `trading.io.breaker.cancel_all_open_orders` |
| `trading.engine._alert_critical` | `trading.critical._alert_critical` |
| `trading.engine._mode` | `trading.critical._mode` |
| `trading.engine._state_store` / `._state_store.X` | `trading.state_store` / `.X` |
| `trading.engine.handle_order_update` / `order_direction` / `advance_order_state_from_status` | `trading.order_state.*` |
| `trading.engine.load_universe` / `_load_universe` / `_load_df_upto` | `trading.data_ctx.*`（按调研 §1 re-export 源） |
| `trading.engine.eod_plan` / `sanity_check_date_alignment` | `trading.eod_plan.*` |
| `trading.engine.pre_open` / `_pre_open_impl` | `trading.phases.pre_open.*` |
| `trading.engine.stop_loss_monitor` / `_scan_expired_positions` / `_close_expired_positions` | `trading.phases.stop_loss.*` |
| `trading.engine.post_close` / `_seq_for_real_oid` / `_order_state_to_db` | `trading.phases.post_close.*` |
| `trading.engine.place_take_profit` | `trading.phases.exit.place_take_profit` |
| `trading.engine._trading_days_between` / `should_trigger_stop` | `trading.compute.stop.*` |
| `trading.engine.decide_exit` | `strategies.neckline.execution.decide_exit` |
| `trading.engine._position_book` | `trading.position_book` |
| `trading.engine.clock` | `trading.clock` |
| `trading.engine.get_gateway` / `_submit` | `trading.gateway_service.get_gateway` / `.submit_order`（下沉后） |
| `trading.engine._resolve_account_id` | `trading.account.resolve_account_id` |
| `trading.engine._last_quote_blackout_alert_ts` | 经 ports.blackout（删 patch 或改 ports） |

> **注**：`TradingEngine` 类 patch（8 处）+ `monkeypatch.setattr(engine, "X", ...)` 形式（159 处）同映射改（setattr 目标改物理模块对象）。Task 8 建全量映射校验脚本。

---

## Phase 0：环验证 + blackout 形态确认

### Task 1：验证 order_state→phases.post_close 无环 + 定 seq/order_state_to_db 真身归宿

**Files:** 读 `trading/order_state.py` / `trading/phases/post_close.py` / `trading/phases/stop_loss.py`

- [ ] **Step 1:** grep `from trading.order_state\|import order_state` in `trading/phases/`。若 post_close/stop_loss 已 import order_state → order_state 再 import phases.post_close 会成环。
- [ ] **Step 2:** 据结果裁决（plan 内决策门）：
  - **无环**（phases 未 import order_state）→ seq_for_real_oid/order_state_to_db 真身留 post_close.py，order_state 直接 `from trading.phases.post_close import ...`（Task 6）。
  - **有环** → 把 `seq_for_real_oid` + `order_state_to_db` 物理真身下沉到 `trading/order_id_seq.py`（新叶子），post_close + order_state 都 import 它。
- [ ] **Step 3:** 结论记 commit message + 进 Task 2。无代码改动（纯验证 Task）。

---

## Phase 1：物理迁移

### Task 2：trading_service.py 下沉 → trading/gateway_service.py

**Files:**
- Create: `trading/gateway_service.py`（1:1 搬迁 `presentation/server/services/trading_service.py` 全文）
- Delete: `presentation/server/services/trading_service.py`（或留 deprecation re-export？**用户定：4 处改指**，故直接删）
- Modify: `presentation/server/api/v1/ops.py:16` / `api/v1/trading.py:28` / `main.py:559` / `services/review_service.py:31`（4 处改指 `trading.gateway_service`）
- Modify: `trading/engine.py:363,381,1292` / `eod_plan.py:225` / `order_state.py:478` / `io/orders.py:43` / `phases/post_close.py:237,344`（8 处 lazy import 改指 `trading.gateway_service`）

**Interfaces:**
- Produces: `trading.gateway_service`（get_gateway/get_status/get_positions/submit_order/cancel_order/get_orders/get_asset/get_jobs/emergency_halt/connect_gateway/disconnect_gateway/aggregate_fills_by_symbol/export_trades/query_trades/record_position_attribution/clear_position_attribution + _gateway_singleton 单例 + 内部 helpers）

- [ ] **Step 1:** 建 `trading/gateway_service.py`，1:1 复制 trading_service.py 全文。
- [ ] **Step 2:** **删孤儿 import** `from presentation.server.http.config import PROJECT_ROOT`（L27，无使用点，调研 §5 确认）。
- [ ] **Step 3:** 改 trading 内 8 处 lazy import：`from presentation.server.services.trading_service import X` → `from trading.gateway_service import X`（engine.py 3 处 + eod_plan/order_state/io.orders/post_close×2）。**转顶层 import**（循环随 _eng_mod 切断解除；若仍环暂保 lazy）。
- [ ] **Step 4:** 改 presentation 4 处调用方 import → `trading.gateway_service`。
- [ ] **Step 5:** 删 `presentation/server/services/trading_service.py`。
- [ ] **Step 6:** 跑 `python -c "import trading.gateway_service; import trading.engine"` 验证无环。
- [ ] **Step 7:** 跑 `pytest tests/trading/ -q` + `tests/presentation/`（若存在）确认绿。
- [ ] **Step 8:** Commit `refactor(trading): trading_service 下沉 trading/gateway_service——切 presentation 反查（W1-A/T2）`

### Task 3：QuoteBlackoutThrottle 收口 + Ports 扩字段

**Files:**
- Create: `trading/alerting.py`（QuoteBlackoutThrottle dataclass）
- Modify: `trading/ports.py`（加 `blackout` 字段）
- Modify: `trading/engine.py:304-305`（删模块级 `_last_quote_blackout_alert_ts`/`_QUOTE_BLACKOUT_ALERT_INTERVAL_S`）+ L623 构造（注入 throttle）
- Modify: `trading/phases/stop_loss.py:276-278`（经 `ports.blackout` 读+写）

- [ ] **Step 1:** TDD——写 QuoteBlackoutThrottle 测试（record/maybe_alert 节流语义：INTERVAL 内不重复告警）。
- [ ] **Step 2:** 实现 `trading/alerting.py`：`@dataclass class QuoteBlackoutThrottle: last_ts: float = 0.0; interval: float = 1800.0; def should_alert(self, now: float) -> bool: ...; def mark(self, now: float) -> None: ...`（线程安全用 lock，与 critical 同范式）。
- [ ] **Step 3:** ports.py 加 `blackout: QuoteBlackoutThrottle`（dataclass 字段，默认 `field(default_factory=QuoteBlackoutThrottle)`）。
- [ ] **Step 4:** engine.py 删 L304-305 模块级状态 + L623 构造 `blackout=QuoteBlackoutThrottle()`（或 self._blackout 传 ports）。stop_loss 入口加 ports 参数（若stop_loss_monitor 不接 ports，**Task 3a：给 stop_loss_monitor/scan_expired/close_expired 加 ports 参数**——破坏性但必要，注入点同 pre_open）。
- [ ] **Step 5:** stop_loss.py 改经 `ports.blackout.should_alert(now)` / `mark(now)` 替 `_eng_mod._last_quote_blackout_alert_ts` 读写。
- [ ] **Step 6:** L4 双跑（stop_loss 行为等价）+ `pytest tests/trading/ -q` 绿。
- [ ] **Step 7:** Commit `refactor(trading): blackout throttle 收口 QuoteBlackoutThrottle——切模块级可变状态（W1-A/T2）`

> **注**：stop_loss 加 ports 参数是本 plan 最具破坏性的接口变更（stop_loss_monitor 被 engine cron 调，调用点 engine.py:~1400 改传 ports）。Step 4 谨慎，L4 双跑必过。

---

## Phase 2：切断 _eng_mod 反查（phases/order_state 改直接 import）

> **总纪律**：每个 Task 切断一组符号后，跑 `python -c "import trading.phases.X"` 逐模块 + `pytest tests/trading/ -q` + L4 双跑（关键路径）。_eng_mod lazy import 语句在该模块所有符号切断后才删（最后一步）。

### Task 4：无状态符号直接 import（9 符号，4 模块）

**符号**：`_state_store` / `_mode` / `_alert_critical` / `qmt_market_data` / `_trading_days_between` / `_cancel_all_open_orders` / `calendar` / `decide_exit`（+ `_resolve_account_id` 并入 Task 5）

**Files:** `phases/{pre_open,stop_loss,post_close,exit}.py` / `order_state.py`

- [ ] **Step 1:** 每个模块顶部加直接 import（按符号→物理路径表）：
  ```python
  from trading import state_store, qmt_market_data, calendar
  from trading.critical import _mode, _alert_critical
  from trading.compute.stop import trading_days_between as _trading_days_between
  from trading.io.breaker import cancel_all_open_orders as _cancel_all_open_orders
  from strategies.neckline.execution import decide_exit
  ```
  （按各模块实际用到的符号加，不全加）
- [ ] **Step 2:** 删各模块内 `_eng_mod._state_store` → `state_store` 等替换（20+11+5+3+3+3+2+1 处，按符号分布）。
- [ ] **Step 3:** 每模块跑 `python -c "import trading.phases.X"` 验证无环 + `pytest tests/trading/ -q`（注：此时测试 patch 仍指 trading.engine——因 re-export 还在，测试仍绿）。
- [ ] **Step 4:** L4 双跑 pre_open/stop_loss（行为等价）。
- [ ] **Step 5:** Commit `refactor(trading): phases 无状态符号切断 _eng_mod 反查——直接 import 叶子（W1-A/T2）`

### Task 5：_resolve_account_id 9 处反查切断

**Files:** `phases/{pre_open:186,exit:91,stop_loss:199/610,post_close:182}.py` / `order_state.py:354`

- [ ] **Step 1:** 各模块顶部 `from trading.account import resolve_account_id as _resolve_account_id`。
- [ ] **Step 2:** 删 `_resolve_account_id = _eng_mod._resolve_account_id` 局部别名（5 处）+ 用法不变（已用 `_resolve_account_id` 名）。
- [ ] **Step 3:** `pytest tests/trading/ -q` 绿 + L4 双跑。
- [ ] **Step 4:** Commit `refactor(trading): phases _resolve_account_id 反查切断——直指 account SSoT（W1-A/T2）`

### Task 6：phases 内部互调（6 符号）+ order_state→phases（依赖 Task 1 裁决）

**符号**：`place_take_profit`（exit） / `_pre_open_impl`（pre_open 同模块） / `scan_expired_positions`/`close_expired_positions`（stop_loss） / `seq_for_real_oid`/`order_state_to_db`（post_close，**按 Task 1 裁决**）

- [ ] **Step 1:** pre_open.py：删 `_eng_mod._pre_open_impl` → 直接 `_pre_open_impl`（同模块）；`_eng_mod._scan_expired_positions`/`_close_expired_positions` → `from trading.phases.stop_loss import scan_expired_positions as _scan_expired_positions, close_expired_positions as _close_expired_positions`。
- [ ] **Step 2:** order_state.py：`_eng_mod.place_take_profit` → `from trading.phases.exit import place_take_profit`；`_eng_mod._seq_for_real_oid`/`_order_state_to_db` → 按Task 1：无环则 `from trading.phases.post_close import ...`，有环则 `from trading.order_id_seq import ...`（Task 1 已下沉真身）。
- [ ] **Step 3:** 逐模块 `python -c "import ..."` 无环验证（**重点 order_state→phases 方向**）。
- [ ] **Step 4:** `pytest tests/trading/ -q` 绿 + L4 双跑。
- [ ] **Step 5:** Commit `refactor(trading): phases 内部互调切断 _eng_mod——同包直接 import（W1-A/T2）`

### Task 7：_submit/get_gateway 反查切断（随 Task 2 下沉）

**Files:** `phases/{pre_open,stop_loss,exit,post_close}.py`（_eng_mod._submit/_eng_mod.get_gateway 共 10 处）

- [ ] **Step 1:** phases 内 `_eng_mod.get_gateway` → `from trading.gateway_service import get_gateway`（顶层）；`_eng_mod._submit` → `from trading.gateway_service import submit_order as _submit`。
- [ ] **Step 2:** 删剩余 `_eng_mod.` 引用（此时各模块 _eng_mod 应已无引用）。
- [ ] **Step 3:** **删各模块的 `import trading.engine as _eng_mod` lazy 语句**（_eng_mod 反查正式切断）。
- [ ] **Step 4:** `grep -rn "_eng_mod" trading/`（排除 .pyc）→ **应为 0 命中**（W1-A 验收线）。
- [ ] **Step 5:** L4 双跑全 phases + `pytest tests/trading/ -q` 绿。
- [ ] **Step 6:** Commit `refactor(trading): _submit/get_gateway 切断 + 删 _eng_mod lazy import——反查清零（W1-A/T2 验收）`

---

## Phase 3：删 re-export 块 + 迁 360 patch（最大工作量）

### Task 8：建 patch 全量映射校验脚本 + 删 re-export 块

**Files:** `trading/engine.py`（删 L78-262 re-export 块）+ 新建 `tests/trading/_patch_audit.py`（临时校验，commit 前删）

- [ ] **Step 1:** 写脚本扫 `tests/` 全量 `patch("trading.engine.X")` + `monkeypatch.setattr(engine, "X")`，输出 {文件: [(行, 旧路径, 新路径)]}，按映射表（plan 上表）自动建议新路径；标无法映射的（如 TradingEngine 类 patch）。
- [ ] **Step 2:** 跑脚本，确认 360 处全部能映射（或标特殊处理）。
- [ ] **Step 3:** 删 engine.py L78-262 re-export 块（保留 engine 真正自身定义的符号）。
- [ ] **Step 4:** 跑 `python -c "import trading.engine"` 确认 engine 自身不依赖 re-export。
- [ ] **Step 5:** 此时 `pytest tests/trading/ -q` 会**大量红**（patch 路径失效）——预期，下批 Task 修。
- [ ] **Step 6:** Commit `refactor(trading): 删 engine re-export 块——patch 迁移前置（W1-A/T2）`

### Task 9-18：按 top 10 文件分批迁 patch（每文件一 Task）

> **模板**（每 Task 同结构，X = 文件名）：
> - [ ] **Step 1:** 跑 Task 8 脚本对 `tests/trading/X.py`，输出该文件所有待迁 patch。
> - [ ] **Step 2:** 按映射表逐个改 `patch("trading.engine.Y")` → `patch("物理.Y")`；`monkeypatch.setattr(engine, "Y", ...)` → `monkeypatch.setattr(物理模块, "Y", ...)`（顶部加 `import 物理`）。
> - [ ] **Step 3:** `pytest tests/trading/X.py -q` 绿。
> - [ ] **Step 4:** Commit `test(trading): X patch 迁物理路径——re-export 删连带（W1-A/T2）`

**Task 9:** `test_pre_open_l1_halt.py`（26 处）
**Task 10:** `test_qmt_health_guard.py`（22 处）
**Task 11:** `test_engine_pre_open_gate.py`（16 处）
**Task 12:** `test_engine_order_update_handler.py`（16 处）
**Task 13:** `test_stoploss_post_close_gate.py`（14 处）
**Task 14:** `test_l2_aggregated_critical.py`（11 处）
**Task 15:** `test_stop_loss_monitor_decide_exit.py`（10 处）
**Task 16:** `test_engine_stoploss_inject.py`（10 处）
**Task 17:** `test_engine_bootstrap.py`（9 处）
**Task 18:** `test_pre_open_ledger.py`（8 处）

### Task 19：迁剩余 patch + monkeypatch.setattr 形式（~218 处）

**Files:** tests/ 其余文件（含 monkeypatch.setattr(engine,...) 159 处 + top10 外 patch ~59 处）

- [ ] **Step 1:** 跑 Task 8 脚本对 tests/ 全量剩余，分文件批次（每 2-3 文件一 commit）。
- [ ] **Step 2:** 按映射表迁。重点：`monkeypatch.setattr(engine, "X", ...)` 顶部 `import trading.engine as engine` 要换成对应物理模块 import（或保留 engine import 若该测试仍 patch engine 自身符号）。
- [ ] **Step 3:** `pytest tests/trading/ -q` 全绿。
- [ ] **Step 4:** 删 Task 8 临时校验脚本。
- [ ] **Step 5:** Commit `test(trading): 剩余 patch 迁物理路径——360 patch 迁移收尾（W1-A/T2）`

---

## Phase 4：验证 + 收尾

### Task 20：W1-A 验收门（_eng_mod grep 0 + e2e + 全量 + 双跑）

- [ ] **Step 1:** `grep -rn "_eng_mod" trading/ tests/`（排除 .pyc）→ **0 命中**（W1-A 核心验收）。
- [ ] **Step 2:** `grep -rn "import trading.engine as " trading/`（phases/order_state lazy import）→ 0（除 engine 自身）。
- [ ] **Step 3:** L3 e2e_long_cycle（4.9min）绿。
- [ ] **Step 4:** L1 全量绿（`pytest tests/ -q`，PYTHONIOENCODING=utf-8）。2 pre-existing 红（test_probabilistic_broker）除外。
- [ ] **Step 5:** L4 双跑（重构前后 eod_plan→pre_open→post_close 全链路 diff=0）——重构用 Task 7 前的 commit 基线。
- [ ] **Step 6:** 模块依赖边权复核（#02 扫描脚本：trading→presentation 边权 → 0；data→trading 不变）。
- [ ] **Step 7:** Commit `test(trading): W1-A T2 验收——_eng_mod grep 0 + e2e + 双跑全绿`

### Task 21：文档订正

**Files:** `docs/architecture/06-tech-debt.md` / `docs/architecture/02-module-dependencies.md`

- [ ] **Step 1:** 06-tech-debt.md：T2（_eng_mod 切断 + trading_service 下沉）标「✅ 已治理（2026-08-12）」。
- [ ] **Step 2:** 02-module-dependencies.md：trading→presentation 边权更新（→0）+ trading 内部拓扑（phases 直接 import）。
- [ ] **Step 3:** Commit `docs(arch): T2 keystone 闭合——06-tech-debt + 02-module-deps 订正`

---

## Self-Review

**1. Spec 覆盖**：
- ✅ 切断 _eng_mod 反查（spec §4.1.1）→ Task 4-7（19 符号差异化）。
- ✅ 下沉 trading_service（spec §4.1.2）→ Task 2（8 lazy import + presentation 4 处）。
- ✅ _resolve_account_id 收口（spec §4.1.3）→ Task 5（9 处反查，SSoT 已就位）。
- ✅ 删 re-export + 迁 patch（用户决策 DG-A 彻底路径）→ Task 8-19。
- ✅ 模块级状态收口（spec §4.1.1 表「模块级可变状态」）→ Task 3（QuoteBlackoutThrottle）。
- ✅ 验收 _eng_mod grep 0（spec §4.1）→ Task 20。

**2. 风险点**：
- Task 3 stop_loss 加 ports 参数（破坏性接口变更）→ L4 双跑 + 调用点 engine.py:~1400 同步改。
- Task 1 order_state→phases 环验证 → 决定 seq/order_state_to_db 真身归宿（无环直 import / 有环下沉 order_id_seq.py）。
- Task 8-19 360 patch 迁移 → 按映射表 + 批次 + 每批绿门；Task 8 校验脚本防漏。
- 循环 import → 每 Task `python -c "import ..."` 验证。

**3. 行号基准**：2026-08-12（调研 re-verified）。ports 构造 L623、_last_quote_blackout_alert_ts L304、lazy import 8 处行号均调研确认。实施时 re-verify。

**4. 行为等价**：每切断 Task L4 双跑；patch 迁移只改路径不改断言；删 re-export 前先全量映射确认（Task 8）。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-08-12-t2-keystone.md`。**这是 spec 最大工作量/最高风险项**（21 Task，含 360 patch 迁移）。建议 Subagent-Driven（每 Task fresh subagent + review，关键路径 L4 双跑）。Task 1（环验证）须先做，其结论影响 Task 6。

执行方式？

---

## 执行偏离注记（2026-08-12 实施后）

> 本段由 controller 在 W1-A/T2 实施后补记，如实反映 3 处 plan 偏离（spec 缩减）。**无实现错误**——偏离理由见各 Task commit messages + `docs/architecture/06-tech-debt.md` + `.superpowers/sdd/2026-08-12-t2-keystone/progress.md`。

### 偏离 1：Task 8 Step 3「删 engine.py L78-262 re-export 块」未执行（defer W1-B）

- **Plan 预期**（Task 8 Step 3, L207）：删 engine.py L78-262 re-export 块，仅保留 engine 自身定义的符号。
- **实际**：re-export 块保留。**Controller 裁定 defer W1-B**——engine 内部 L262 之后仍大量经模块全局名读 re-export 符号（eod_plan×21 / pre_open×44 / post_close×22 / clock×17），删 re-export 会 break engine 内部 import 链，需先把 engine 内部这些「经全局名读 re-export」的反查点改成直 import 物理真身（属 W1-B 范畴）。
- **W1-A 验收无回退**：Task 20 Step 1「`grep _eng_mod` → 0」已达成——_eng_mod 反查（phases/order_state 经 `_eng_mod.X` 读 engine 符号）与「engine 内部经模块全局名读 re-export」是两条不同路径，后者不是 _eng_mod 反查，不影响 W1-A 验收门。

### 偏离 2：Task 19「360 patch 全迁」部分执行（仅迁 fail 相关 patch）

- **Plan 预期**（Task 19, L231-239）：剩余 ~218 处 `patch("trading.engine.X")` + `monkeypatch.setattr(engine, "X")` 全迁物理路径，HEAD 后零 `patch engine.X`。
- **实际**：HEAD 仍 ~124 处 `patch engine.X`。策略裁调为「仅迁 fail 相关 patch」——删 re-export 块 defer（偏离 1）后，未迁 patch 仍能经 re-export 命中，测试不红，故无强制全迁的验收压力。
- **留守 patch 性质**：多数是合法 C3/C4（patch engine 自持符号，非 re-export），如 `get_gateway`×40 / `load_plan`×11 / `calendar`×8 / `TradingEngine`×8 等——这些符号 engine 本身定义或持有，`patch engine.X` 是正确路径，不需迁。
- **follow-up**：删 re-export 块（W1-B）后，真正 orphaned 的 patch（patch 的是已删 re-export 符号）会在 W1-B 显形并一并迁；本次 silently orphaned patch 已记 `06-tech-debt.md` follow-up。

### 偏离 3：Task 6 Step 2「order_state 顶部 import」回退函数内 lazy

- **Plan 预期**（Task 6 Step 2, L181）：order_state.py `_eng_mod.place_take_profit` / `_seq_for_real_oid` / `_order_state_to_db` → 顶部直接 `from trading.phases.{exit,post_close} import ...`。
- **实际**：三处均为**函数内 lazy import**（order_state.py L527 / L602 / L654）——跨包环阻断顶部 import：`broker.base` → `trading.__init__` → `order_state` → `phases.exit` → `gateway_service` → `broker.base`，顶部 import 会触发 `broker.qmt` 先加载致环。
- **预授权依据**：plan L116 Task 2 Step 3 通用兜底「**转顶层 import**（循环随 _eng_mod 切断解除；若仍环暂保 lazy）」——该精神对 Task 6 Step 2 同样适用（_eng_mod 已切断，但跨包环未随切断解除，故暂保 lazy）。
- **行为等价**：函数内 lazy import 在 call-time 绑定 local 名，运行时语义与顶部 import 等价（仅 import 时机推迟到首次调用）；测试 patch 直打 `order_state.place_take_profit` 等物理路径，不经 engine 反查。
