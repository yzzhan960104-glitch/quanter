# 核心依赖：量化计算与数据处理
pandas>=2.0.0
numpy>=1.24.0

# 机器学习依赖（HMM 宏观状态识别）
hmmlearn>=0.3.0
scikit-learn>=1.3.0

# 可视化依赖
plotly>=5.14.0
matplotlib>=3.7.0

# 数据获取（预置，后续可扩展）
yfinance>=0.2.28

# ============ 真实数据源依赖（双轨数据架构） ============

# 美联储经济数据 (FRED) API 客户端
fredapi>=0.5.0

# A 股数据源 (Tushare Pro)
tushare>=1.4.0
# tnskhdata：Tushare 代理库（API 完全兼容，10000 积分，优先于直连 tushare）。
# data/_tushare_compat.get_pro() 在 TNSKHDATA_TOKEN 配置时走代理分支；stock_basic/
# daily_basic/daily/adj_factor 等接口需其高积分解锁，直连 tushare 积分不足拉不动。
# 纯 Python wheel（py3-none-any），3.10 venv 可直接装。依赖 bs4/lxml/pandas/requests。
tnskhdata>=0.1.1

# 环境变量加载（API 凭证隔离，绝不硬编码）
python-dotenv>=1.0.0

# ============ Web 全栈依赖（第一阶段新增） ============

# 后端 API 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# FastAPI TestClient 的 HTTP 传输层依赖（API 路由测试必需，否则
# starlette.testclient 无法导入；fastapi 未将其声明为强依赖，故显式列出）
httpx>=0.27.0

# 开发工具
pytest>=7.4.0
# pytest-asyncio：钉钉桥异步测试依赖（ClaudeProcess/ClaudePool/stream_client
# 均为 asyncio 协程，需 @pytest.mark.asyncio 识别；strict 模式下显式标注）。
pytest-asyncio>=0.23.0
black>=23.7.0
pylint>=2.17.0

# 类型检查
mypy>=1.5.0

# ===== 工业级蜕变新增依赖 =====
pyarrow>=14.0
# openai 已移除：LLM 情感路径已摒弃，不再引入 OpenAI/GLM 客户端
aiohttp>=3.9
celery[redis]>=5.3
redis>=5.0
psutil>=5.9
tqdm>=4.66

# ===== 宏观 CTA 究极重构：数据源切轨（替代 Tushare，引入 JQData 分钟级） =====
# AKShare：开源 A 股/宏观/板块数据，无 Token 门槛，规避 Tushare 积分限制；
# 锁定 1.18.64 是本会话已验证可用的版本，避免上游 API 字段漂移。
akshare==1.18.64
# JQData：分钟级 K 线与财务数据源，独立配额计数防超额扣费。
jqdatasdk==1.9.8
# thriftpy2：jqdatasdk 的 RPC 传输底层，显式锁定版本避免与服务端协议失配。
thriftpy2==0.4.20
# fastparquet：分钟级数据落盘的另一 Parquet 后端（pyarrow 已在依赖中），
# 保留作为多湖写入的备选引擎，提升极端场景兼容性。
fastparquet

# ===== 蔡森形态学流水线 =====
# 注：以下依赖因 PyPI 上游元数据 bug / py3.10 版本约束，实际安装状态见 task-0-report.md。
# 形态学核心算法（因果 ZigZag / 颈线 / W底 / 头肩底）一律用 numpy/pandas 显式实现，
# 遵循 CLAUDE.md「第一性原理、拒绝黑盒」原则，不强依赖第三方形态库。
mplfinance>=0.12.9b7            # K 线可视化（PyPI 实际版本 0.12.10b0，约束带 b 后缀以匹配）
lightweight-charts>=2.0         # 交互式图表（PyPI 真名 lightweight-charts，非 -python）
# —— 以下三项 PyPI 装不上（上游 bug / py3.10 约束），注释掉以保 pip install -r 幂等性 ——
# 形态学算法改用 numpy/pandas 显式实现（因果 ZigZag / ATR / 颈线），符合极简无黑盒原则。
# zigzag>=0.2.0                  # PyPI 装不上（sdist 元数据 bug：Cython>=^0.29 + 缺 .pyx），改用 numpy 显式因果 ZigZag
# pandas-ta>=0.3.14b             # PyPI 装不上（py3.10 下无可用版本，0.4.x 要求 >=3.12），改用 pandas 显式 ATR/指标
# trendln>=4.0.0                 # PyPI 装不上（最高仅 0.1.18，4.0.0 版本号不存在），改用 numpy polyfit 显式颈线/趋势线

# ===== 钉钉远程驱动 Claude 旁路桥（dws-migration Task 5/6 已退役）=====
# 原 dingtalk-stream>=0.20.0 已移除：bridge/ 自研全退役，两机器人改走 dws dev connect
# （对话机器人 yzzhanCli 通用 → dws dev connect --channel claudecode；
#  审查机器人 yzzhan参数优化 → dws 桥 + webhook 推报告）。SDK 无用户。
