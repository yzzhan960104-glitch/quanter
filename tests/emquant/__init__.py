# -*- coding: utf-8 -*-
"""tests/emquant：东财掘金颈线单文件试点（spec 2026-08-21）测试包。

分层原则：本包测试只消费 emquant/ 下的【静态定稿产物】（json/单文件源），不重跑
导出器、不触 data_lake / 实验 DB / .env——产物型任务的测试钉"快照不变质"，
而不是钉"导出器今天还能跑"（后者依赖数据时效，属导出器自身的导出期断言职责）。
"""
