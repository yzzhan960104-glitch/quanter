# ============================================================================
# 前端 API 鉴权 token（构建期注入，对齐后端 QUANTER_API_TOKEN）
# ============================================================================
# 用途：本值经请求拦截器（src/api/client.ts）注入 `Authorization: Bearer <token>`，
#       与后端 server/core/auth.py 的 require_write 校验对齐。
#
# 对齐红线：本值必须与后端环境变量 QUANTER_API_TOKEN【字面量完全一致】，
#          否则生产环境所有 trading/caisen/data/review 请求（含受保护 router 的只读
#          请求，如 GET /caisen/plans）一律 401，前端写操作全瘫。
#
# 安全边界（量化风控·极度拷问）：
#   - VITE_ 前缀变量会被打进前端 bundle，等同对「能访问前端」的用户公开；
#   - 仅适用于内网/单用户自部署（与后端静态 Bearer token 设计同前提）；
#   - 公网部署必须叠加后端 QUANTER_ALLOWED_IPS 白名单，或改由反向代理层注入，
#     严禁在此裸填生产密钥后直接公网暴露。
#
# 使用方式：复制为 web/.env.local 填入真实值（.env.local 已在 .gitignore，不入库）。
# 本地开发：留空=不注入，后端开发态（未配 QUANTER_API_TOKEN）同样放行，零摩擦。
VITE_API_TOKEN=
